import { defineConfig } from 'tsdown'

/**
 * Build the package entrypoints and the PDF parser as separate bundles. The parser is loaded by
 * file inside a resource-limited Worker and therefore must not share a generated runtime chunk.
 */
export default defineConfig([
  {
    entry: ['lib/types/index.js'],
    outDir: 'lib',
    format: ['esm'],
    platform: 'node',
    target: 'es2024',
    fixedExtension: false,
    dts: false,
    clean: false,
  },
  {
    entry: ['lib/types/pdf-worker.js'],
    outDir: 'lib',
    format: ['cjs'],
    platform: 'node',
    target: 'es2024',
    fixedExtension: false,
    dts: false,
    clean: false,
  },
])
