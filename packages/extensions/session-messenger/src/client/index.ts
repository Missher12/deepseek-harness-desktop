/** Placeholder Client entry; Task 5 owns the footer contribution. */
export const name = 'session-messenger-client'
/** No Client services are required until the notification surface is installed. */
export const inject: readonly string[] = []
/** Keep the independently buildable Client face inert during the core-only stage. */
export function apply(): void {}
