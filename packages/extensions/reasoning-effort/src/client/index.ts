/**
 * No-feature Client entry for the reasoning-effort package scaffold.
 * @module @deepseek-ai/dsh-reasoning-effort/client
 */

/** Cordis Client plugin name. */
export const name = 'reasoning-effort-client'

/**
 * Apply the scaffold without registering Client behavior.
 * @returns Nothing; the scaffold has no Client effects.
 */
export function apply(): void {}
