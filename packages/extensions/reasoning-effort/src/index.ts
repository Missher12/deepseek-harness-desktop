/**
 * No-feature Host entry for the reasoning-effort package scaffold.
 * @module @deepseek-ai/dsh-reasoning-effort
 */

/** Cordis Host plugin name. */
export const name = 'reasoning-effort'

/**
 * Apply the scaffold without registering Host behavior.
 * @returns Nothing; the scaffold has no Host effects.
 */
export function apply(): void {}
