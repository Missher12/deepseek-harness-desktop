/**
 * Attachment UI locale constants.
 *
 * The fallback type token is language-neutral and derived from a sanitized
 * display name; it stays here so presentation source owns no embedded copy.
 */
export const DOCUMENT_TYPE_FALLBACK = 'FILE'
