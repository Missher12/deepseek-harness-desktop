# Desktop 0.4.3 Computer Use Implementation Plan

English | [中文](2026-08-27-desktop-0.4.3-computer-use.zh.md)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (<code>- [ ]</code>) syntax for tracking.

**Goal:** Ship one-source Desktop 0.4.3 for Intel macOS and Windows x64 with the user-approved Codex-style prompt rail, structured Agent control of the built-in browser, and visible, revocable multi-application Computer Use.

**Architecture:** Keep model tools inside Harness and privileged execution inside Electron main plus an on-demand native helper. Two typed service seams, <code>browserControl</code> and <code>computerControl</code>, cross the unique parent/child Node IPC channel through <code>desktop-control-protocol</code>, the sole source of every cross-process action, result, and branded ID DTO. Electron owns leases, quotas, and policy, while the native helper mirrors and rechecks them. Browser actions use an in-process CDP wrapper with opaque revision-bound refs; native actions prefer AX/UIA refs and use coordinates only for vision-capable models.

**Tech Stack:** TypeScript, React, CSS Modules, Cordis, Zod/Schemastery, Electron WebContentsView/CDP, Node child-process IPC, Rust, macOS AXUIElement/ScreenCaptureKit/CGEvent, Windows UI Automation/Windows Graphics Capture/SendInput, Vitest, Playwright snapshots, electron-builder, GitHub Actions.

---

## Delivery order

Tasks 1–2 are an independent prompt-rail slice. Tasks 3–8 deliver a complete browser-control slice. Task 9 has hard prerequisites on Tasks 3–6; Tasks 9–13 add Computer Use in the order observe → stop/cleanup acceptance → input. Task 14 changes version and release metadata only after every earlier gate is green.

### Task 1: Make the prompt rail visibly Codex-like

**Files:**
- Modify: <code>packages/client/ui-conversation/src/client/chat/PromptRail.tsx</code>
- Modify: <code>packages/client/ui-conversation/src/client/chat/ChatView.module.css</code>
- Modify: <code>packages/client/ui-conversation/src/client/locales.ts</code>
- Modify: <code>packages/client/ui-conversation/tests/chat-view.client.spec.tsx</code>
- Create: <code>packages/client/ui-conversation/tests/prompt-rail-style.client.spec.ts</code>

- [ ] **Step 1: Add failing tests for valid theme tokens, the approved active marker, hover text, and nearest-anchor pointer selection.**

~~~ts
import { expect } from 'vitest'

declare const css: string
declare const promptRailScreen: { getByRole(role: string): { textContent: string | null } }
declare function nearestPromptIndex(input: { y: number; top: number; height: number; count: number }): number

expect(css).not.toContain('--dsw-alias-label-quaternary')
expect(css).toContain('var(--dsw-alias-label-tertiary,')
expect(nearestPromptIndex({ y: 260, top: 0, height: 520, count: 120 })).toBe(60)
expect(promptRailScreen.getByRole('tooltip').textContent).toBe('第 12 条发言')
~~~

- [ ] **Step 2: Run <code>pnpm exec vitest run packages/client/ui-conversation/tests/chat-view.client.spec.tsx packages/client/ui-conversation/tests/prompt-rail-style.client.spec.ts</code>; verify RED on the undefined token and absent nearest-anchor helper.**
- [ ] **Step 3: Replace the invalid color token with a defined token and fallback; implement the approved 28–32 px gutter, visible neutral ticks, DeepSeek-blue active tick and hollow dot, two-line tooltip, and current/total count.**
- [ ] **Step 4: Move pointer hit testing to the rail track. Export a pure clamped <code>nearestPromptIndex</code>; keep tick buttons for semantic labels but prevent overlapping pointer hitboxes from owning clicks.**
- [ ] **Step 5: Re-run the focused tests; expect PASS and no edit, fork, rewind, delete, draft, or send calls.**
- [ ] **Step 6: Commit with <code>fix(conversation): make prompt rail visible and precise</code>.**

### Task 2: Add keyboard and narrow-layout access

**Files:**
- Modify: <code>packages/client/ui-conversation/src/client/chat/PromptRail.tsx</code>
- Modify: <code>packages/client/ui-conversation/src/client/chat/ChatView.module.css</code>
- Modify: <code>packages/client/ui-conversation/src/client/locales.ts</code>
- Modify: <code>packages/client/ui-conversation/tests/chat-view.client.spec.tsx</code>
- Create: <code>apps/web/tests/prompt-rail.e2e.ts</code>
- Create: intentional light/dark prompt-rail snapshots under <code>apps/web/tests/snapshots/</code>

- [ ] **Step 1: Add failing tests for roving tabindex, ArrowUp/ArrowDown/Home/End, Enter activation, forced-colors visibility, and a compact Prompt Navigation button below 860 px.**
- [ ] **Step 2: Run the component suite and replay snapshot test; verify RED because the rail currently disappears at 860 px and every tick owns an overlapping button.**
- [ ] **Step 3: Implement one keyboard focus owner and a compact popover list for narrow layouts. Preserve exact <code>onActivate(seq)</code> navigation and reduced-motion behavior.**
- [ ] **Step 4: Record only the approved light/dark snapshots, then run <code>pnpm run test:gui</code> and <code>DSH_SNAPSHOT=replay pnpm run test:web</code>.**
- [ ] **Step 5: Commit with <code>feat(conversation): keep prompt navigation accessible</code>.**

### Task 3: Define the closed Desktop-control wire protocol

**Files:**
- Create: <code>packages/control/desktop-control-protocol/package.json</code>
- Create: <code>packages/control/desktop-control-protocol/tsconfig.json</code>
- Create: <code>packages/control/desktop-control-protocol/src/brand.ts</code>
- Create: <code>packages/control/desktop-control-protocol/src/bridge.ts</code>
- Create: <code>packages/control/desktop-control-protocol/src/helper.ts</code>
- Create: <code>packages/control/desktop-control-protocol/src/codec.ts</code>
- Create: <code>packages/control/desktop-control-protocol/src/index.ts</code>
- Create: <code>packages/control/desktop-control-protocol/src/invariant.ts</code>
- Create: <code>packages/control/desktop-control-protocol/protocol-v1.json</code>
- Create: <code>packages/control/desktop-control-protocol/tests/codec.spec.ts</code>
- Create: shared raw <code>packages/control/desktop-control-protocol/fixtures/*.bin</code> frames and a tiny PNG.
- Create: package README bilingual pair and pairing record.

- [ ] **Step 1: Write failing codec/framing tests for protocol version 1; all 27 bridge kinds; exact internal acquire/release field and result matrices; surface-kind/capability/target constraints; pair-preserving helper targets; total and category quotas; protocol-version, duplicate-key, and strict unknown-field rejection; exact 49,152-byte semantic text, 65,536-byte JSON payload, 65,537-byte JSON frame, 4,194,304-byte PNG body, and 4,194,321-byte PNG frame edges; zero/oversized length prefixes before allocation; split length headers/bodies; multiple frames; PNG correlation/hash/dimensions/orphan/late cases; a two-phase JSON direction gate that runs before image-pending state; closed kinds and errors; non-interchangeable brands; frozen nested output; immutable PNG copies; and the forbidden generic field vocabulary. TypeScript must decode and re-encode the shared raw <code>.bin</code> fixtures and tiny PNG byte-for-byte now; Task 9 applies the identical assertion in Rust.**

~~~ts
export const BRIDGE_REQUEST_KINDS = [
  'control.lease.acquire', 'control.lease.release',
  'desktop.status',
  'browser.snapshot', 'browser.navigate', 'browser.click', 'browser.type',
  'browser.key', 'browser.select', 'browser.scroll', 'browser.wait',
  'browser.back', 'browser.forward', 'browser.reload', 'browser.stop',
  'computer.status', 'computer.list', 'computer.snapshot', 'computer.focus',
  'computer.click', 'computer.double-click', 'computer.drag', 'computer.type',
  'computer.key', 'computer.scroll', 'computer.wait', 'computer.stop',
] as const
export const HELPER_REQUEST_KINDS = [
  'status', 'list', 'snapshot', 'focus', 'click', 'double-click', 'drag',
  'type', 'key', 'scroll', 'wait', 'stop', 'lease.install', 'input.release',
] as const
export const CONTROL_KINDS = [
  'request.cancel', 'session.revoke', 'lease.revoke', 'parent.shutdown',
] as const
~~~

- [ ] **Step 2: Run <code>pnpm exec vitest run packages/control/desktop-control-protocol/tests/codec.spec.ts</code>; verify RED because the package is absent.**
- [ ] **Step 3: Make this package the sole source of every cross-boundary action, result, and brand DTO. Check in a machine-readable <code>protocol-v1.json</code> that freezes the complete bridge/helper/control rosters—including all 27 bridge requests—closed field matrices, and every numeric limit once; generate or validate TypeScript against it now, require Task 9 Rust to consume the same manifest and fixtures, and reject any generic schema escape. TypeScript must byte-exactly decode and re-encode every shared raw <code>.bin</code>/tiny-PNG fixture. Define branded <code>RequestId</code>, <code>ControlLeaseId</code>, <code>BrowserRef</code>, <code>ComputerRef</code>, and <code>PngTransferId</code>; import and re-export the official <code>SessionId</code>; and carry <code>leaseRevision</code>. The top-level <code>messageKind</code> is exactly <code>request</code>, <code>response</code>, or <code>control</code>; responses are exactly <code>ok</code> or <code>error</code>, echo <code>requestKind</code>, and errors use only <code>NOT_SUPPORTED</code>, <code>UNAUTHORIZED</code>, <code>LEASE_EXPIRED</code>, <code>LEASE_REVOKED</code>, <code>STALE_REF</code>, <code>TARGET_CLOSED</code>, <code>PERMISSION_DENIED</code>, <code>POLICY_DENIED</code>, <code>CONTROL_DISABLED</code>, <code>TARGET_NOT_AUTHORIZED</code>, <code>APPROVAL_DENIED</code>, <code>DUPLICATE_REQUEST</code>, <code>TOO_MANY_PENDING</code>, <code>QUOTA_EXCEEDED</code>, <code>BINARY_MISMATCH</code>, <code>BUSY</code>, <code>TIMEOUT</code>, <code>CANCELLED</code>, <code>DISCONNECTED</code>, and <code>INTERNAL</code>, with a UTF-8 message of at most 512 bytes and boolean <code>retryable</code>.**
- [ ] **Step 4: Implement strict immutable decoding and the one binary envelope. The Rust four-byte big-endian length includes tag plus body; reject zero or values above 4,194,321 before allocation. Expose a two-phase decode/correlate seam (or an equivalent direction predicate) so Task 5 can direction-check a validated JSON message before the correlator records image metadata and waits for PNG. Every bridge request carries the official session/request/deadline base. Add internal-only <code>control.lease.acquire</code> fields for one closed <code>surfaceKind</code>, a duplicate-free closed <code>capabilities</code> array, and pair-preserving <code>targets: { appId, windowIds[] }[]</code>. Browser targets are empty; native targets contain at least one current window per app and obey the manifest's app/window/list/identifier/JSON limits. Its immutable result contains only lease ID/revision, surface kind, effective targets/capabilities, and idle/hard relative durations. Add <code>control.lease.release</code> with lease ID/revision and exact <code>{ released: true }</code> result. Neither accepts nor returns quotas, clocks, <code>approved</code>, or digest; model DTOs cannot emit them. Helper requests carry the Electron-shortened duration as <code>timeoutMs</code>; lease-scoped requests add <code>leaseId</code>/<code>leaseRevision</code>, and only target-scoped requests add <code>appId</code>/<code>windowId</code>/<code>snapshotRevision</code>. <code>lease.install</code> carries pair-preserving targets and Electron-authored quotas with total <code>operations</code> plus snapshots/pointer/key/text categories; <code>agentId</code> is display-only. <code>input.release</code> has no lease fields, is approval-free and Electron-only, and cannot be emitted by child/model DTOs. Reject generic <code>command</code>, <code>script</code>, <code>selector</code>, <code>javascript</code>, <code>method</code>, <code>args</code>, <code>env</code>, <code>cwd</code>, <code>path</code>, <code>channel</code>, <code>ipc</code>, <code>payload: unknown</code>, <code>data: unknown</code>, <code>Record</code>, and index signatures. Malformed, unknown, wrong-version, oversized, or mis-sequenced input closes only the control channel. Stateful direction allowlists, duplicate request IDs, late replies, cancellation, pending limits, tombstones, and child generations belong to Task 5, not this codec.**
- [ ] **Step 5: Commit with <code>feat(control): define closed desktop protocol</code>.**

### Task 4: Define Browser Control and Computer Control services

**Files:**
- Create: <code>packages/control/browser-control/</code> with <code>src/index.ts</code>, <code>src/types.ts</code>, <code>src/invariant.ts</code>, tests, manifest, tsconfig, and README pair.
- Create: <code>packages/control/computer-control/</code> with <code>src/index.ts</code>, <code>src/types.ts</code>, <code>src/policy.ts</code>, <code>src/invariant.ts</code>, tests, manifest, tsconfig, and README pair.

- [ ] **Step 1: Add failing Service tests for stable registration, duplicate-provider rejection, internal acquire, shared-client/cache behavior, exact model-schema exclusion of authority fields, session revocation, stale refs, action limits, secure fields, sensitive action classification, and typed immutable snapshot envelopes whose metadata and PNG are present or absent together. Verify transfer ID, byte length, SHA-256, and dimensions remain correlated and every PNG read returns a copy.**

~~~text
export abstract class BrowserControl extends Service {
  abstract acquireLease(request: ControlLeaseAcquireRequest, signal: AbortSignal): Promise<ControlLeaseAcquireResult>
  abstract snapshot(request: BrowserSnapshotRequest, signal: AbortSignal): Promise<BrowserSnapshotEnvelope>
  abstract act(request: BrowserActionRequest, signal: AbortSignal): Promise<BrowserActionResult>
  abstract revokeSession(sessionId: SessionId): Promise<void>
}

export type BrowserSnapshotWithoutImage = Omit<BrowserSnapshotResult, 'image'> & {
  readonly image?: never
}
export type BrowserSnapshotWithImage = Omit<BrowserSnapshotResult, 'image'> & {
  readonly image: NonNullable<BrowserSnapshotResult['image']>
}
export type BrowserSnapshotEnvelope =
  | Readonly<{ readonly result: BrowserSnapshotWithoutImage; readonly png?: never }>
  | Readonly<{ readonly result: BrowserSnapshotWithImage; readonly png: ImmutablePng }>
~~~

~~~text
export abstract class ComputerControl extends Service {
  abstract acquireLease(request: ControlLeaseAcquireRequest, signal: AbortSignal): Promise<ControlLeaseAcquireResult>
  abstract status(): Promise<ComputerControlStatus>
  abstract list(request: ComputerListRequest, signal: AbortSignal): Promise<ComputerListResult>
  abstract snapshot(request: ComputerSnapshotRequest, signal: AbortSignal): Promise<ComputerSnapshotEnvelope>
  abstract act(request: ComputerActionRequest, signal: AbortSignal): Promise<ComputerActionResult>
  abstract stop(sessionId: SessionId): Promise<void>
}

export type ComputerSnapshotWithoutImage = Omit<ComputerSnapshotResult, 'image'> & {
  readonly image?: never
}
export type ComputerSnapshotWithImage = Omit<ComputerSnapshotResult, 'image'> & {
  readonly image: NonNullable<ComputerSnapshotResult['image']>
}
export type ComputerSnapshotEnvelope =
  | Readonly<{ readonly result: ComputerSnapshotWithoutImage; readonly png?: never }>
  | Readonly<{ readonly result: ComputerSnapshotWithImage; readonly png: ImmutablePng }>
~~~

- [ ] **Step 2: Run both package suites; verify RED before implementing the services.**
- [ ] **Step 3: Reuse or re-export all protocol action, result, lease-acquire, brand, PNG metadata, and <code>ImmutablePng</code> types; do not copy their declarations into either service package. Both seams expose internal <code>acquireLease</code> with the protocol DTO/result. Their Host providers share the one process-wide IPC client and lease cache, derive the official session and transport fields, acquire before an ordinary operation, and never project acquire, session ID, lease ID/revision, approval, quota, clock, or digest fields into a model tool schema. Do not add a third Service. Add immutable bounded service-only snapshot envelopes and ref types, binding browser refs to surface + revision and computer refs to app + process + window + revision + scale. A snapshot envelope exposes protocol metadata plus encapsulated <code>ImmutablePng</code>, never mutable raw bytes, and enforces <code>result.image === undefined</code> exactly when <code>png === undefined</code>.**
- [ ] **Step 4: Implement policy results <code>ALLOW</code>, <code>APPROVAL_REQUIRED</code>, and <code>DENY</code>. Known secure text, OTP, payment, file, biometric, password-manager, and OS privacy/security targets always return DENY; uncertain sensitivity fails closed.**
- [ ] **Step 5: Re-run tests and commit with <code>feat(control): add browser and computer seams</code>.**

### Task 5: Bridge the Harness child to Electron main

**Files:**
- Create: <code>packages/control/desktop-control-host/package.json</code>, <code>tsconfig.json</code>, <code>src/ipc-client.ts</code>, <code>src/browser-provider.ts</code>, <code>src/computer-provider.ts</code>, <code>src/index.ts</code>, <code>src/invariant.ts</code>, focused tests, and README bilingual pair/record.
- Create: <code>apps/desktop/src/control/bridge-server.ts</code> and <code>apps/desktop/tests/control-bridge.spec.ts</code>.
- Create: <code>apps/desktop/tests/fixtures/harness-ipc-child.mjs</code> as a real spawned-child IPC fixture.
- Modify: <code>apps/desktop/src/harness/process.ts</code>, <code>apps/desktop/tests/process.spec.ts</code>, <code>apps/desktop/src/main.ts</code>, and <code>apps/desktop/tsdown.config.ts</code>.
- Modify: <code>apps/desktop/desktop.cordis.patch.yml</code>, <code>apps/desktop/package.json</code>, <code>apps/desktop/tsconfig.json</code>, and <code>apps/desktop/tests/manifest.spec.ts</code>.
- Modify: <code>scripts/stage-desktop.ts</code>, <code>scripts/stage-desktop.spec.ts</code>, <code>scripts/verify-package-readme-model-experience.ts</code>, <code>tsconfig.host.json</code>, and <code>pnpm-lock.yaml</code>.
- Modify: <code>packages/control/README.md</code>, its Chinese counterpart/pairing record, and the generated module-graph bilingual pair/record.

- [ ] **Step 1: Add failing package and Desktop tests for one process-wide client and lease cache shared by both providers; the two internal lease requests; exact link-and-direction allowlists checked before image correlation; immediate control-channel closure for a wrong-direction image response even when no PNG follows; one copied raw <code>Uint8Array</code> frame per advanced-serialization message with no length prefix; atomic JSON-plus-optional-PNG envelopes; request correlation; the strict <code>now < deadline <= now + 30,000</code> boundary and valid shortening; duplicate IDs; cancellation; awaited <code>agent/turn-stopping</code> release plus <code>turn/end</code>/<code>session/flush</code> fallback tails; session and exact lease revocation; timeout; late replies; exactly 32 pending requests; exactly 256 terminal tombstones; child generations; disconnect rejection; listener cleanup; and zero protocol-content logging. Responses must match the pending <code>requestId</code> and <code>requestKind</code>; cancellation must also match its official <code>sessionId</code>; and lease revocation must exactly match session, lease ID, and lease revision derived from the validated pending request.**
- [ ] **Step 2: Run the host package, bridge, process, manifest, and staging tests; verify RED while Harness uses only stdin/stdout/stderr, the Desktop patch has no host provider, and the stage lacks its runtime files.**
- [ ] **Step 3: Preserve <code>spawn()</code> and change the exact owned child to <code>stdio: ['ignore', 'pipe', 'pipe', 'ipc']</code> with <code>serialization: 'advanced'</code>. Copy the ambient environment, explicitly remove inherited <code>NODE_CHANNEL_FD</code> and <code>NODE_CHANNEL_SERIALIZATION_MODE</code>, and let Node create fd 3 and its internal channel plumbing solely from the <code>ipc</code> stdio entry; never assign either variable, manually open fd 3, or switch to <code>fork()</code>. Expose typed send/message/disconnect hooks only on the exact child owned by <code>HarnessProcess</code>. Add an awaited <code>beforeStop</code> hook inside <code>HarnessProcess.stop()</code> so its own startup-failure cleanup and every retry/quit caller pass through the same control shutdown. Prove the real fixture exchanges raw <code>Uint8Array</code> frames in both directions and that the Node link never adds the helper link's four-byte length prefix. Do not add a loopback port, remote-debugging port, renderer relay, or environment bearer token.**

~~~text
const childEnv = { ...process.env }
delete childEnv.NODE_CHANNEL_FD
delete childEnv.NODE_CHANNEL_SERIALIZATION_MODE
childEnv.ELECTRON_RUN_AS_NODE = '1'

const child = spawn(executable, args, {
  cwd: workspace,
  detached: platform !== 'win32',
  env: childEnv,
  stdio: ['ignore', 'pipe', 'pipe', 'ipc'],
  serialization: 'advanced',
})
~~~

- [ ] **Step 4: Enforce direction after strict JSON decoding but before dispatch or entry into image-correlation pending state. Harness → Electron accepts all 27 <code>BRIDGE_REQUEST_KINDS</code>, including internal-only <code>control.lease.acquire</code>/<code>control.lease.release</code>, plus only same-session/request <code>request.cancel</code> and session-disposal <code>session.revoke</code>; it rejects every response, PNG, helper-only request, <code>lease.revoke</code>, and <code>parent.shutdown</code>. Electron → Harness accepts only the corresponding closed bridge response, its immediately adjacent screenshot PNG, <code>lease.revoke</code>, and <code>parent.shutdown</code>; it rejects every request, <code>request.cancel</code>, and <code>session.revoke</code>. A wrong-direction image-bearing response closes the control channel immediately without waiting for its declared PNG. Unknown, malformed, orphan-PNG, or wrong-direction input closes only this control channel and leaves Harness/Web/chat running. Keep the helper directions frozen for Task 9: Electron → helper allows all 14 helper requests and all four Electron-authored high-priority controls; helper → Electron allows only matching helper responses and adjacent screenshot PNG.**
- [ ] **Step 5: Implement a ledger with at most 32 pending entries and an insertion-ordered FIFO tombstone set capped at exactly 256 terminal request IDs. Bind each entry to <code>{ requestId, requestKind, sessionId, generation, deadline, cancellation, lease? }</code>, where optional <code>lease: { leaseId, leaseRevision }</code> is derived only from a validated lease-bearing request; reject duplicate live or tombstoned IDs; settle only an exact ID/kind match; require cancel session matches; and discard all late terminal responses. Capture <code>nowUnixMs</code> once and accept only <code>nowUnixMs < deadlineUnixMs <= nowUnixMs + 30,000</code>; never clamp an invalid deadline, and convert only a positive shortened remainder to <code>timeoutMs</code>. An exact <code>lease.revoke</code> synchronously invalidates the matching shared cache, settles only entries with the same session/lease ID/revision exactly once as <code>LEASE_REVOKED</code>, tombstones them once, and leaves different sessions, lease IDs, revisions, and unleased work untouched. Repeated revoke is idempotent without refreshing FIFO order; late responses are discarded. Abort, timeout, disconnect, <code>parent.shutdown</code>, and dispose likewise settle affected pending work exactly once.**
- [ ] **Step 6: Serialize whole envelopes through one send-callback queue on each peer. Copy every input frame before enqueue, send a JSON frame and its optional correlated PNG without interleaving another envelope, and advance only from the send callback. Guard inbound handling, async provider completion, timers/abort continuations, and send callbacks with both the exact child identity and generation. Increment generation before replacement listeners become active; an old completion may only discard its detached copy. A process-level <code>disconnect</code> clears the control queues/listeners and rejects pending work with <code>DISCONNECTED</code> but must not terminate Harness or impair chat.**
- [ ] **Step 7: Mount one <code>desktop-control-host</code> instance in the immutable Desktop-only Cordis patch. Its single IPC client and lease cache back both Browser Control and Computer Control providers; never create one listener, ledger, or authority cache per provider. The providers derive official session/transport fields internally and do not register lease requests as tools. On the normal path, <code>agent/turn-stopping</code> synchronously invalidates the cache and awaits a bounded <code>control.lease.release</code> tail. Because <code>session/event</code> is post-commit fire-and-forget and error/cancel paths may skip that awaited event, its <code>turn/end</code> observer only invalidates synchronously and enqueues an idempotent fallback tail; it cannot promise release before the <code>turn/end</code> commit. An awaited <code>session/flush</code> listener drains the tail before the next checkpointed model/top-level-tool side effect. <code>session/disposed</code> invalidates and enqueues <code>session.revoke</code>; the plugin effect disposer drains every release/revoke tail before removing listeners. Transport failures settle cleanup listeners and leave control unavailable rather than breaking ordinary chat.**

- [ ] **Step 8: Add the package to the Desktop dependency and project-reference closure, Host aggregate, lockfile, package-group docs/model-experience classification, generated module graph, staging required-file allowlist, manifest assertions, and stage tests. Keep <code>@deepseek-ai/dsh-desktop-control-protocol</code> in the Desktop-main <code>neverBundle</code> list. Before deleting or constructing a stage, validate the one canonical Host patch row. Require the staged protocol <code>package.json</code>, <code>lib/index.js</code>, <code>lib/invariant.js</code>, and <code>protocol-v1.json</code>; Host <code>package.json</code>, <code>lib/index.js</code>, and <code>lib/invariant.js</code>; and Browser/Computer Service <code>package.json</code>, <code>lib/index.js</code>, and <code>lib/invariant.js</code>. Import built Desktop main from a stage-shaped temporary tree to prove the protocol manifest resolves. A missing control link leaves providers unavailable without breaking ordinary Harness startup.**
- [ ] **Step 9: Keep lease authority and native-input release in Task 6. An acquire response is withheld until Electron's native challenge and any required helper <code>lease.install</code> acknowledgement succeed. A release response returns <code>released: true</code> only after revoke, queue clearing, downstream browser/helper cleanup, and known-input release acknowledgement. For normal shutdown, stop admission and await the same Task 6 cleanup first. Then the awaited <code>HarnessProcess.beforeStop</code> hook enqueues <code>parent.shutdown</code>, waits for its callback within a bound, disconnects the exact IPC link, and detaches listeners before process-tree termination. The same hook is reached by <code>HarnessProcess.start()</code>'s internal startup-failure cleanup, retry, and quit. Transport logs may contain only direction, generation, bounded counts, and closed reason codes—never frame bytes, decoded requests/results, semantic/page/field text, PNGs, cookies, tokens, or absolute user paths.**
- [ ] **Step 10: Re-run focused tests plus Host/Desktop typechecks, package/constraint/publint gates, translation pairing, generated-doc freshness, exact staging checks, the stage-shaped built-main import, and the real spawn fixture. Commit only after those gates pass with <code>feat(desktop): bridge control over owned child ipc</code>.**

### Task 6: Add the authoritative ControlLease and sensitive policy

**Files:**
- Create: <code>apps/desktop/src/control/control-lease.ts</code>
- Create: <code>apps/desktop/src/control/control-coordinator.ts</code>
- Create: <code>apps/desktop/src/control/native-approval.ts</code>
- Create: <code>apps/desktop/src/control/action-grant.ts</code>
- Create: <code>apps/desktop/src/control/policy.ts</code>
- Create: <code>apps/desktop/src/control/settings-store.ts</code>
- Create: <code>apps/desktop/src/control/audit.ts</code>
- Create: <code>apps/desktop/src/control/emergency-shortcut.ts</code>
- Create: <code>apps/desktop/tests/control-lease.spec.ts</code>
- Create: <code>apps/desktop/tests/control-coordinator.spec.ts</code>
- Create: <code>apps/desktop/tests/control-native-approval.spec.ts</code>
- Create: <code>apps/desktop/tests/control-action-grant.spec.ts</code>
- Create: <code>apps/desktop/tests/control-security.spec.ts</code>
- Create: <code>apps/desktop/tests/control-settings-store.spec.ts</code>
- Create: <code>apps/desktop/tests/control-audit.spec.ts</code>
- Create: <code>apps/desktop/tests/control-emergency-shortcut.spec.ts</code>
- Modify: <code>apps/desktop/src/application.ts</code>
- Modify: <code>apps/desktop/src/main.ts</code>
- Modify: <code>apps/desktop/tests/main-lifecycle.spec.ts</code>

- [ ] **Step 1: Add failing monotonic fake-clock tests for the single process-wide active lease; official session/pair-preserving target/capability binding; Electron-authored total and category quotas; safe-integer monotonic <code>leaseRevision</code> with overflow denial; exact <code>>=</code> five-minute idle and twenty-minute hard expiry; generation-bound timers; and effective helper timeout equal to <code>min(valid positive bridge remaining, 30s, hard remaining)</code>. Only accepted lease-scoped actions refresh idle time and hard expiry never moves. Cover task completion; awaited normal-path release at <code>agent/turn-stopping</code>; error/cancel fallback queued from post-commit <code>turn/end</code> and drained by <code>session/flush</code>; session switch/disposal and disposer drain; app restart; user Stop; disconnect; close-to-tray; quit; retry; startup failure; helper crash; and foreign-session rejection. Do not claim release precedes the <code>turn/end</code> commit on fallback paths. Prove child/model data cannot carry <code>approved</code>, mint a lease, propose quotas, advance its revision, extend a deadline/lease clock, or combine an allowed app with another app's window.**

~~~text
interface ControlLease {
  id: ControlLeaseId
  sessionId: SessionId
  leaseRevision: number
  agentId: string
  surfaceKind: 'browser-ephemeral' | 'browser-human-persistent' | 'native-application'
  targets: readonly { readonly appId: string; readonly windowIds: readonly string[] }[]
  capabilities: ReadonlySet<'observe' | 'pointer' | 'keyboard'>
  quotas: ControlLeaseQuotaSnapshot
  issuedAt: number
  lastActionAt: number
  hardExpiresAt: number
}
~~~

- [ ] **Step 2: Run lease/security tests; verify RED because no authority exists.**
- [ ] **Step 3: Implement one Electron-main coordinator as authority. It intersects the requested desired subset with the current enumeration, ordinary app allowlist, and fixed policy, then owns one global pending native dialog and at most one process-wide active lease. A missing or hidden owner window, Escape, close, abort, exception, or competing request denies. Late approval revalidates official session, target, revision, and allowlist. Harness <code>ctx.approval</code> may request interaction and supply UX but cannot authorize anything. Treat installed Host plugins in the same Harness Node process as part of the trusted computing base; claim only that renderer/page/model data cannot forge Electron confirmation. A renderer Give action creates only a main-owned intent for the next official child acquire and cannot mint a lease or carry session authority.**
- [ ] **Step 4: Install the Electron-authored pair-preserving lease snapshot through <code>lease.install</code> and return no descriptor until the helper acknowledgement succeeds. Charge total <code>operations</code> once before every lease-scoped dispatch, then the existing snapshot/pointer/key/text category before each applicable native call. Treat <code>agentId</code> as display-only. Make <code>lease.revoke</code> idempotent, approval-free, and higher priority than the action queue. Return <code>released: true</code> only after the awaited revoke → clear queues → browser/helper cleanup → release known input → release ack sequence. The full helper shutdown continues with stdin EOF → await helper → bounded terminate/kill ladder.**
- [ ] **Step 5: Implement one-shot action grants for every persistent-human-browser mutation: <code>navigate</code>, <code>click</code>, <code>type</code>, <code>key</code>, <code>select</code>, <code>scroll</code>, <code>back</code>, <code>forward</code>, and <code>reload</code>. Electron computes the exact action digest, shows and verifies its native challenge, consumes the grant once, and rechecks session/target/revision immediately before dispatch. Page/model/renderer/child cannot submit or consume a digest or grant. Stop is always approval-free.**
- [ ] **Step 6: Persist settings through an independent strict schema and atomic file replacement. Store only ordinary app allowlist entries, ordinary switches, and the accelerator; never store window IDs, leases, refs, digests, screenshots, or session authority. Rotate audit rows containing only time, salted session hash, opaque app ID, action class, and outcome—never page/field text, URL/query, paths, screenshots, cookies, tokens, or secrets.**
- [ ] **Step 7: Register the emergency accelerator only while a lease is active and fail the grant closed when registration fails. Rebind atomically, unregister only this feature's owned accelerator, and never call <code>unregisterAll</code>. The callback synchronously revokes authority and closes admission before awaiting cleanup. Prevent close-to-tray from hiding the window until active-control cleanup completes. Make <code>DesktopApplication</code> await control cleanup before Harness stop on session switch/disposal, disconnect, quit, retry, and helper crash; document that power loss or simultaneously killing Electron/helper cannot guarantee release.**
- [ ] **Step 8: Re-run focused tests for monotonic exact boundaries, overall quota, dialog pending/races, shortcut failure/rebind, renderer forgery, <code>ctx.approval</code> non-authority, helper-install failure, persistent-browser action grants, and every awaited lifecycle path, plus Desktop typecheck and repository gates. Commit with <code>feat(desktop): enforce control leases and policy</code>.**

### Task 7: Implement the semantic browser adapter

**Files:**
- Create: <code>apps/desktop/src/browser/cdp.ts</code>
- Create: <code>apps/desktop/src/browser/agent-controller.ts</code>
- Create: <code>apps/desktop/src/browser/surface-manager.ts</code>
- Create: <code>apps/desktop/src/browser/policy.ts</code>
- Create: <code>apps/desktop/tests/browser-agent.spec.ts</code>
- Create: <code>apps/desktop/tests/browser-policy.spec.ts</code>
- Modify: <code>apps/desktop/src/browser/contracts.ts</code>
- Modify: <code>apps/desktop/src/browser/controller.ts</code>
- Modify: <code>apps/desktop/tests/browser-contracts.spec.ts</code>

- [ ] **Step 1: Add failing tests with a fake <code>webContents.debugger</code> for foreign-debugger <code>BUSY</code> without detach, owned detach, main/same-document navigation races, late CDP replies, bounded Accessibility traversal, secure/file denial, stale mount tokens, the correct official session's atomic first-call open and visible mount, cross-session <code>BUSY</code> without reveal or remount, redirect-aware URL SSRF rejection, unique ephemeral partition clearing, deterministic high-DPI/oversized-viewport screenshot reduction and PNG validation, popup/new-window/download/permission/clipboard/file-chooser denial with handler teardown, and ref-only click/type/key/select/scroll/wait.**
- [ ] **Step 2: Run browser tests; verify RED on missing Agent APIs and the current persistent partition.**
- [ ] **Step 3: Use an in-process CDP wrapper only. Start with <code>Accessibility.getRootAXNode</code> and perform bounded breadth-first <code>Accessibility.getChildAXNodes</code> traversal; allow read-only <code>DOM.describeNode</code> only for <code>type</code>/<code>autocomplete</code>/<code>disabled</code>/<code>readonly</code>, plus <code>DOM.getBoxModel</code>, fixed Input methods, <code>Page.captureScreenshot</code>, and always-on <code>Page.setInterceptFileChooserDialog</code> solely to suppress native choosers. Never use or expose <code>Runtime</code>, JavaScript evaluation, selectors, arbitrary CDP methods, file-setting methods, or debugger access. Omit editable values by default and deny uncertain sensitivity.**
- [ ] **Step 4: Implement one Electron-main <code>BrowserSurfaceManager</code> with a global exclusive Agent surface/lease, surface generation, and mount token. On the first provider call for official session S, after the coordinator resolves any valid persistent Give intent, atomically bind that instance to S or create a unique non-persistent S-owned surface and visibly mount it before dispatch. Reuse a surface only for the exact owner session and generation; derive ownership from the trusted provider request, never the renderer route or selected workbench chat. If another session owns the global surface, return <code>BUSY</code> without revealing, hiding, or remounting it; stale hide callbacks cannot affect a replacement. Keep the human signed-in partition persistent and transfer its currently visible instance only after Give to Agent. Track <code>attachedByUs</code>; a foreign debugger returns <code>BUSY</code> and is never detached. Stop awaits detach, view cleanup, partition clearing, and revocation.**
- [ ] **Step 5: Cap traversal at 2,000 raw nodes, depth 32, 512 CDP calls, 2,000 ms wall time, 300 actionable output nodes, 49,152 UTF-8 semantic bytes, a 65,536-byte encoded JSON payload, and 4,194,304 PNG bytes. Capture only the visible viewport with <code>captureBeyondViewport: false</code>; before capture, choose a deterministic scale no greater than 1 so neither PNG edge exceeds 2,048 pixels and decoded area does not exceed 4,194,304 pixels. Allow at most three total attempts, reducing scale geometrically after an oversized encoding; parse and validate the final PNG signature, IHDR dimensions, declared dimensions, byte length, and SHA-256 before attachment, and fail closed if it still exceeds any limit. Detach, destroy, main-document navigation, same-document navigation, and material tree change invalidate refs and late replies. Browser actions accept refs only; no browser coordinate action exists. Restrict <code>browser.wait</code> to <code>duration</code>, <code>navigation</code>, or <code>loading-idle</code>, with duration capped at 10,000 ms. Reject URL userinfo and every loopback/link-local/private initial or redirect destination unless explicitly user-allowlisted. Before navigation, install a deny-all <code>setWindowOpenHandler</code>, cancel <code>will-download</code>, and install both permission-check and permission-request handlers that deny camera, microphone, location, clipboard read/write, and every other permission. Intercept every file chooser without a file-setting path; reject file inputs and uploads. Remove only this manager generation's handlers during awaited teardown.**
- [ ] **Step 6: Re-run tests and commit with <code>feat(desktop): add semantic browser adapter</code>.**

### Task 8: Expose Browser Control tools and visible takeover UI

**Files:**
- Create: <code>packages/control/tool-browser-control/</code> with snapshot/action/presentation modules, tests, manifest, tsconfig, README pair.
- Modify: <code>packages/extensions/desktop-workbench/src/client/BrowserMode.tsx</code>
- Modify: <code>packages/extensions/desktop-workbench/src/client/BrowserMode.module.css</code>
- Modify: <code>packages/extensions/desktop-workbench/src/client/locales.ts</code>
- Modify: <code>packages/extensions/desktop-workbench/src/client/index.tsx</code>
- Modify: <code>packages/extensions/desktop-workbench/src/client/preferences.ts</code>
- Modify: <code>packages/extensions/desktop-workbench/package.json</code> and client/host tsconfigs.
- Create: <code>packages/extensions/desktop-workbench/tests/browser-mode.client.spec.tsx</code>
- Modify: <code>apps/desktop/src/preload-api.ts</code>
- Modify: <code>apps/desktop/src/preload.ts</code>
- Modify: <code>apps/desktop/src/preferences.ts</code>
- Modify: <code>apps/desktop/package.json</code> and <code>apps/desktop/tsconfig.json</code>
- Modify: <code>apps/cli/package.json</code> and <code>apps/cli/tsconfig.json</code>
- Modify: <code>apps/desktop/tests/preload-api.spec.ts</code>
- Modify: <code>apps/desktop/desktop.cordis.patch.yml</code>
- Modify: <code>pnpm-lock.yaml</code> and the generated module-graph bilingual pair/record.
- Modify: appropriate non-minimal Desktop agent presets and their e2e tests.

- [ ] **Step 1: Add failing tests for the independent closed tools <code>browser_snapshot</code>, <code>browser_navigate</code>, <code>browser_click</code>, <code>browser_type</code>, <code>browser_key</code>, <code>browser_select</code>, <code>browser_scroll</code>, <code>browser_wait</code>, <code>browser_back</code>, <code>browser_forward</code>, <code>browser_reload</code>, and <code>browser_stop</code>. Prove wait modes and duration are closed and capped, and no tool accepts coordinates, files, uploads, or chooser handles.**
- [ ] **Step 2: Assert all browser models use refs only; vision-capable models may save bounded PNG bytes through <code>ctx.attachments.saveImage()</code> and return an ImageBlock, but screenshots never enable coordinate actions.**
- [ ] **Step 3: Deny known password/OTP/payment/file targets. Document that page JavaScript can give ordinary actions external effects and AX cannot classify every outcome. The ephemeral surface task lease covers only the fixed ordinary roster. On a persistent human surface, every mutating <code>navigate</code>/<code>click</code>/<code>type</code>/<code>key</code>/<code>select</code>/<code>scroll</code>/<code>back</code>/<code>forward</code>/<code>reload</code> requires its own one-shot Electron-native challenge bound to the exact action digest. <code>ctx.approval.request()</code> may render UX but is not authorization; tool schemas never accept <code>approved</code>, and page/model/renderer/child cannot supply, consume, or reuse a digest or grant. Stop is always approval-free.**
- [ ] **Step 4: Add Give to Agent / Stop takeover beside the browser toolbar. Show a signed-in-state warning before transferring the current visible persistent instance. Expose each action as a no-argument preload method: any supplied argument, unknown field, prototype-bearing object, or coercible value is rejected. Electron main accepts only the current visible owner window's main-frame sender, derives the official session, surface, generation, and mount token itself, and revalidates them when the next official child acquire consumes Give or when Stop executes. Give retains only a main-owned user intent; renderer/preload cannot mint a lease, choose session authority, name a partition/webContents/URL, or expose model action primitives. Stop remains pending until manager cleanup finishes.**
- [ ] **Step 5: Register tools conditionally only when a provider exists, so ordinary CLI/Web boot unchanged without one. Run focused tools, strict preload/IPC/preference validators for wrong sender/session, stale mount token, extra arguments, unknown/prototype fields, and coercion, workbench UI, preset, GUI, first-call correct-session mount, cross-session non-reveal, foreign-debugger/detach/navigation-race/mount-token/SSRF/late-CDP/partition-clearing, popup/new-window/download/permission-check/permission-request/clipboard/file-chooser denial and teardown, oversized/high-DPI screenshot reduction, provider-absent CLI/Web, and fixture browser-loop tests. Complete <code>navigate → snapshot → click → type → wait → back → stop</code> locally.**
- [ ] **Step 6: Commit with <code>feat(browser): add visible agent control</code>.**

### Task 9: Build the on-demand Rust helper without input injection

**Files:**
- Create: <code>native/computer-use-helper/.gitignore</code>, workspace <code>Cargo.toml</code>, committed <code>Cargo.lock</code>, and exactly pinned <code>rust-toolchain.toml</code>.
- Create: three crates under <code>native/computer-use-helper/crates/</code>: protocol, platform-neutral core, and helper binary.
- Create: <code>scripts/build-computer-use-helper.ts</code> and its Vitest file.
- Modify: <code>package.json</code>, <code>scripts/stage-desktop.ts</code>, <code>scripts/stage-desktop.spec.ts</code>, and <code>apps/desktop/electron-builder.yml</code>.
- Create: <code>apps/desktop/src/control/helper-process.ts</code> and tests.
- Modify: <code>.github/workflows/desktop-release.yml</code>, <code>.github/workflows/windows-desktop.yml</code>, and <code>THIRD_PARTY_NOTICES.md</code>.

- [ ] **Step 1: Do not start until Tasks 3–6 pass. Add Rust tests that validate the checked-in v1 manifest and re-encode the exact shared raw <code>.bin</code>/tiny-PNG fixture bytes. Reject duplicate JSON keys, unknown fields, wrong versions, zero/oversized prefixes before allocation, malformed refs, and invalid JSON/PNG correlation, hash, dimensions, ordering, orphaning, or lateness; cover split headers/bodies and multiple frames.**
- [ ] **Step 2: Add failing TS tests for one on-demand child, bounded framed I/O, immediate <code>timeoutMs</code>-to-monotonic conversion, immutable PNG copies, crash mapping, stdin EOF cleanup, and zero helper process while idle. Validate Mach-O/PE architecture independently in the build script, staging, and packaged smoke.**
- [ ] **Step 3: Implement <code>serde(deny_unknown_fields)</code> in the three-crate workspace. The read-only slice executes only <code>status</code>, <code>list</code>, <code>snapshot</code>, <code>stop</code>, <code>lease.install</code>, idempotent high-priority <code>lease.revoke</code>, and cleanup. It decodes reserved Electron-only <code>input.release</code> and the frozen input roster but returns <code>NOT_SUPPORTED</code> until native input is enabled. Lease idle/hard expiry uses monotonic clocks only.**
- [ ] **Step 4: Build into exact ignored platform paths <code>apps/desktop/native-bin/darwin-x64/computer-use-helper</code> or <code>apps/desktop/native-bin/win32-x64/computer-use-helper.exe</code>. Stage exactly one matching binary through electron-builder <code>extraResources</code>; missing, wrong-architecture, non-executable, or extra-platform artifacts fail closed.**
- [ ] **Step 5: Pin and explicitly install the Rust toolchain in Mac/Windows workflows; run <code>cargo build --locked</code>, fmt, clippy with warnings denied, Cargo tests, build-script tests, stage tests, all three architecture checks, app main build, and third-party-notice verification.**
- [ ] **Step 6: Commit with <code>feat(computer): add read-only native helper</code>.**

### Task 10: Implement macOS observation and permissions

**Files:**
- Create macOS modules under <code>native/computer-use-helper/crates/helper/src/platform/macos/</code> for accessibility, capture, permissions, scale, and platform composition.
- Create: <code>apps/desktop/src/control/permissions.ts</code> and tests.
- Modify: helper fixture and integration tests.

- [ ] **Step 1: Add platform-independent fake tests for app/window identity, AX role/name/bounds projection, secure-value redaction, scale conversion, capture bounds, permission denied, and stale window refs.**
- [ ] **Step 2: Implement AXUIElement traversal with depth/node/text limits. Never return secure field values or hidden-window content.**
- [ ] **Step 3: Require macOS 14+ and implement authorized-window capture only through <code>SCScreenshotManager</code> with exact-window <code>SCContentFilter</code> and bounded <code>SCStreamConfiguration</code>. CoreGraphics may resolve window identity/coordinates and support CGEvent, but never captures or serves as fallback.**
- [ ] **Step 4: Query Accessibility and Screen Recording permission from the helper that actually calls the APIs. Return manual System Settings guidance; do not automatically mutate privacy settings.**
- [ ] **Step 5: Run Cargo tests and a manual read-only fixture against two harmless apps. Confirm no click, key, or scroll event is emitted.**
- [ ] **Step 6: Commit with <code>feat(computer): observe macos applications</code>.**

### Task 11: Enable macOS bounded input after cleanup gates

**Files:**
- Create: macOS <code>input.rs</code> and input-release tests.
- Modify: helper state, policy, Electron controller, and integration tests.

- [ ] **Step 1: Add failing tests for focus, semantic click, double-click, drag, Unicode type, closed key vocabulary, scroll, cancellation/revocation between every native event, budget deduction before each native call, and release of every held key/button on stop/crash/EOF. Add Electron conservative input-journal tests: after unexpected helper exit, a fresh helper with the same binary hash and, in signed builds, the same signing identity accepts Electron-only <code>input.release</code> containing only known held key/button-up events; child/model attempts are rejected.**
- [ ] **Step 2: Prove secure fields, OTP, biometrics, password managers, privacy settings, and unapproved high-impact controls are denied before any CGEvent call.**
- [ ] **Step 3: Before enabling any CGEvent input, pass native acceptance for awaited revoke → clear queue → release → ack → EOF → await → kill-ladder cleanup on Stop, quit, retry, crash, and session disposal, plus the same-helper recovery release path. Record the explicit power-loss/simultaneous-force-kill limitation.**
- [ ] **Step 4: Implement CGEvent actions only after the helper validates the mirrored live lease and fresh ref. Check revocation between every emitted native event and deduct event/action budgets before each native call. Coordinate fallback requires a vision-capable route and a current screenshot revision.**
- [ ] **Step 5: Complete one visible two-app fixture task; trigger emergency stop and helper crash during drag and typing. Verify only journaled up-events are replayed, no input remains stuck, and no helper survives Harness exit.**
- [ ] **Step 6: Commit with <code>feat(computer): control authorized macos apps</code>.**

### Task 12: Implement the symmetric Windows adapter

**Files:**
- Create Windows modules under <code>native/computer-use-helper/crates/helper/src/platform/windows/</code> for UIA, capture, permissions, scale, input, and platform composition.
- Modify Windows native workflow and packaged-smoke tests.

- [ ] **Step 1: Add Windows-gated tests for HWND/PID plus process-creation identity that rejects PID/HWND reuse, UI Automation projection, secure-value redaction, per-monitor DPI, stale refs, Windows Graphics Capture bounds, complete SendInput release, and denial of UIPI-blocked/high-integrity/secure-desktop targets without elevation.**
- [ ] **Step 2: Implement UIA semantic observation and WGC capture before enabling input. Verify the same protocol fixtures byte-for-byte on Windows.**
- [ ] **Step 3: Implement focus, semantic click, double-click, drag, Unicode type, closed key chords, scroll, stop, and parent-disconnect cleanup through SendInput. Never elevate; reject UIPI, higher-integrity, secure-desktop, or reused-identity targets before input. Recheck revocation between native events and charge budgets before each call.**
- [ ] **Step 4: Run the one Windows workflow once. Complete two harmless-app tasks, sensitive denial, emergency stop, app close, Harness close, uninstall, process cleanup, and data preservation.**
- [ ] **Step 5: Commit with <code>feat(computer): control authorized windows apps</code>.**

### Task 13: Add Computer Use tools, settings, and global stop UI

**Files:**
- Create: <code>packages/control/tool-computer-control/</code> with focused modules/tests/README pair.
- Create: <code>packages/client/ui-desktop-control/</code> with settings, status capsule, store, contracts, locales, styles, and tests.
- Modify: <code>packages/client/ui-layout/src/client/AppFrame.tsx</code> and slot declarations.
- Modify: <code>apps/desktop/src/preload-api.ts</code>, <code>preload.ts</code>, <code>main.ts</code>, menu/tray files, preferences, and tests.
- Modify: <code>apps/desktop/desktop.cordis.patch.yml</code> and staging requirements.

- [ ] **Step 1: Add failing tests for the independent closed tools <code>computer_status</code>, <code>computer_list</code>, <code>computer_snapshot</code>, <code>computer_focus</code>, <code>computer_click</code>, <code>computer_double_click</code>, <code>computer_drag</code>, <code>computer_type</code>, <code>computer_key</code>, <code>computer_scroll</code>, <code>computer_wait</code>, and approval-free <code>computer_stop</code>.**
- [ ] **Step 2: Add failing UI tests for OS permission states, app allowlist, current app/action capsule, Stop, tray/menu Stop, configurable emergency shortcut, and loaded-without-restart marketplace status. Version 0.4.3 has no Pause state, button, or protocol action.**
- [ ] **Step 3: Save screenshots through attachments only for vision-capable routes. Explicitly exclude screenshots/AX text from telemetry, logs, memory, and MSE candidate inputs.**
- [ ] **Step 4: Add the additive layout status slot without taking over the existing overlay slot. Register the global stop shortcut only while control is active and unregister it on stop or app disposal.**
- [ ] **Step 5: Run all control, client, GUI, awaited <code>DesktopApplication</code> shutdown, lifecycle, plugin, memory, MSE, and startup tests. Cover quit/retry/crash/session disposal and prove control shutdown precedes Harness stop. Confirm missing permissions, absent providers, or helper failure never blocks Harness startup or ordinary CLI/Web chat.**
- [ ] **Step 6: Commit with <code>feat(desktop): finish visible computer use</code>.**

### Task 14: Version, document, verify, install, and release 0.4.3

**Files:**
- Modify all three Desktop package manifests, update metadata, version/migration/manifest tests, release workflows, packaged-smoke sentinels, root/Desktop README pairs, <code>PROJECT_CONTEXT.md</code>, generated catalogs, module graphs, third-party notices, and update manifest.
- Add the implemented Agent Note bilingual pair and move any proposed note to implemented.
- Build: <code>DeepSeek-Harness-0.4.3-mac-x64.dmg</code> and <code>DeepSeek-Harness-Setup-0.4.3-win-x64.exe</code>.

- [ ] **Step 1: Change version assertions to 0.4.3 and verify RED before changing production manifests.**
- [ ] **Step 2: Update every Desktop-owned version surface and documentation from the same final source commit. If signing credentials are unavailable, allow an explicitly labeled unsigned/unnotarized test release that promises neither persistent TCC nor SmartScreen trust. If signing is configured, verify nested helper and app signing with the same Team ID, hardened runtime, and notarization; verify the signed Windows helper and signed NSIS installer with an explicit SmartScreen status.**
- [ ] **Step 3: Run focused tests, Cargo fmt/clippy/tests, <code>test:gui</code>, replayed Web tests, typecheck, lint, constraints, generated catalogs, module graphs, documentation sync, secret scans, and clean source builds.**
- [ ] **Step 4: Build and verify the Intel macOS 14+ DMG with temporary <code>DSH_HOME</code>, packaged smoke, nested-helper identity/architecture, <code>hdiutil verify</code>, checksum, browser loop, two-app Computer Use loop, sensitive denial, emergency stop, zero residual processes, and unchanged real data evidence. In the shared native workflow, verify the signed Windows helper/NSIS path when signing is configured; otherwise verify and label the unsigned test path and its SmartScreen limitation.**
- [ ] **Step 5: Back up the installed app, quit only exact owned processes, replace it, verify version/architecture/random-loopback startup/control permissions/data preservation, and retain the recovery backup until acceptance passes.**
- [ ] **Step 6: Push once, dispatch one shared native release workflow from the exact 0.4.3 tag, and do not retry or re-download unchanged artifacts.**
- [ ] **Step 7: Publish only after Mac and Windows native gates pass. Verify uploaded state, exact size, SHA-256, LF checksum files, update manifest bytes/hash/URL, preserved unrelated assets, and one anonymous download per platform.**
- [ ] **Step 8: Commit final evidence with <code>docs(desktop): close 0.4.3 release</code>.**

## Final self-review checklist

- Every requirement in the approved design maps to a task above.
- No task adds rewind, message editing, arbitrary selectors, arbitrary JavaScript, shell-based GUI control, credential reads, silent control, or background screenshots.
- Browser refs, AX/UIA refs, screenshots, leases, and approvals have one authoritative owner and explicit expiry.
- Stop and cleanup are implemented and tested before native input is enabled.
- macOS and Windows ship from one commit but use real native adapters and real native acceptance.
- Versioning and GitHub access happen once at the end, after local gates.
