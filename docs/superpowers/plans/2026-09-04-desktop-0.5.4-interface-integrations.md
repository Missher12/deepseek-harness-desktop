# Desktop 0.5.4 Interface and Integrations Implementation Plan

English | [中文](2026-09-04-desktop-0.5.4-interface-integrations.zh.md)

> **Required skills:** `superpowers:executing-plans`, `superpowers:test-driven-development`, and `superpowers:verification-before-completion`.

**Goal:** Correct the macOS native safe area, move and expose the Turn rail on the left, dock and label the right Workbench, curate `@/+`, and expose the real BrowserSkill/Open Design plugin state without startup probes.

**Architecture:** The commander owns all shared code. Existing first-party Turn, Workbench, input-trigger, BrowserSkill, and Desktop IPC seams remain authoritative. Open Design stays in its isolated official `open-design` profile; Desktop exposes only a sanitized on-demand status.

**Baseline:** `56ab771285b5c00e856036616e257a3414c80c2c` on `codex/desktop-0.5.4-integration`.

### Task 1: Preserve the native titlebar through redirects

**Files:**
- Modify: `apps/desktop/src/preload-api.ts`
- Modify: `apps/desktop/src/preload.ts`
- Modify: `apps/web/src/desktop-api.d.ts`
- Modify: `apps/web/src/desktop-surface.ts`
- Test: `apps/desktop/tests/preload-api.spec.ts`
- Test: `apps/web/tests/desktop-surface.spec.ts`
- Test: `apps/desktop/tests/packaged-smoke.ts`

- [ ] Add a RED test proving a bare-root redirect with a macOS preload bridge preserves `hidden-inset`, while Windows does not gain an inset.
- [ ] Add a closed immutable `presentation.titlebar` preload fact and strict validation; prefer it over the lossy URL query.
- [ ] Add a packaged macOS geometry assertion that all application controls begin below the traffic-light exclusion band.
- [ ] Run focused tests, client/desktop typechecks, lint, and commit `fix(mac): preserve the native titlebar safe area`.

### Task 2: Put the Turn rail on the left and retain normal-window visibility

**Files:**
- Modify: `packages/client/ui-chat/src/client/chat/TurnNavigator.module.css`
- Test: `packages/client/ui-chat/tests/turn-navigator-styles.client.spec.ts`
- Test: `packages/client/ui-chat/tests/turn-navigator.client.spec.tsx`
- Modify: `apps/desktop/tests/packaged-smoke.ts`

- [ ] Write RED stylesheet tests rejecting right anchoring, requiring start-side marks, right-opening preview, and a 680px narrow cutoff.
- [ ] Change CSS only unless a failing behavioral test proves TSX must change. Keep the slot zero-height and transcript geometry unchanged.
- [ ] Add browser/package geometry assertions for left-of-transcript placement, two marks, one active mark, preview/composer non-intersection, and default 1012px visibility.
- [ ] Run focused tests and commit `fix(chat): place the turn rail in the left gutter`.

### Task 3: Curate `@` and `+` around callable capabilities

**Files:**
- Modify: `packages/client/ui-input-trigger/src/types.ts`
- Modify: `packages/client/ui-input-trigger/src/client/MenuView.tsx`
- Modify: `packages/client/ui-input-trigger/src/client/locales.ts`
- Modify: `packages/client/ui-skill/src/client/index.ts`
- Modify: `packages/client/ui-commands/src/client/service.ts`
- Modify: `packages/extensions/ui-cordis/src/client/index.ts`
- Modify: `packages/client/ui-conversation/src/client/input/composer-add-source.ts`
- Test: corresponding input-trigger, skill, command, Cordis, and composer-add specs

- [ ] RED: bare `@` orders Goal/Plan, skills, Cordis plugins, then references; `browser-skill` appears in the first viewport; `+` excludes ordinary commands.
- [ ] Add closed goal/plan/skill/plugin icons and source-owned section labels. Set deterministic source orders without changing `/` command semantics.
- [ ] Keep selection behavior real: skills still insert canonical `/name`; Cordis plugins keep their existing `@pluginId` path; files/sessions keep reference codecs.
- [ ] Run focused tests and commit `feat(composer): prioritize skills in add and mention menus`.

### Task 4: Dock and clarify the Workbench and plugin page

**Files:**
- Modify: `packages/client/ui-layout/src/client/columns.ts`
- Modify: `packages/client/ui-layout/tests/columns.client.spec.ts`
- Modify: `packages/extensions/desktop-workbench/src/client/HeaderButton.tsx`
- Modify: `packages/extensions/desktop-workbench/src/client/WorkbenchPanel.tsx`
- Modify: `packages/extensions/desktop-workbench/src/client/WorkbenchPanel.module.css`
- Modify: `packages/extensions/desktop-workbench/src/client/BrowserSkillMode.tsx`
- Modify: `packages/extensions/desktop-workbench/src/client/locales.ts`
- Create: `apps/desktop/src/integrations.ts`
- Test: `apps/desktop/tests/integrations.spec.ts`
- Modify: `apps/desktop/src/preload-api.ts`, `preload.ts`, and `main.ts`
- Test: Workbench client/style, preload, main-lifecycle, and packaged smoke specs

- [ ] RED: 1012px plus collapsed sidebar yields one 300px-or-wider docked Workbench; narrower frames use the existing drawer. The header trigger includes visible text.
- [ ] Change utility min/default to 300/360px and preserve preference clamps/recovery. Keep one right surface and one tabpanel.
- [ ] Retain persisted `browserSkill` id but label it Plugins. Extend its page with BrowserSkill and Open Design cards.
- [ ] Implement an on-demand, path-free Open Design snapshot from the exact official profile manifest. No manifest read occurs during startup, and BrowserSkill still probes only after Check.
- [ ] Run focused and broad layout/lifecycle tests; commit `feat(desktop): clarify the docked plugin workbench`.

### Task 5: Shared verification and handoff

- [ ] Run host/client/Desktop TypeScript builds, type-aware lint on changed TS/TSX, focused Vitest suites, `git diff --check`, and translation pairing.
- [ ] Stage and package once on macOS with an isolated `DSH_HOME`; run the existing packaged smoke plus 100%/150% screenshots containing only fixtures.
- [ ] Report the shared commit SHA to both platform tasks. They may change only platform-owned native smoke/workflow files and must not restyle shared UI.
