# Desktop 0.4.3 Computer Use 实施计划

[English](2026-08-27-desktop-0.4.3-computer-use.md) | 中文

> **面向 agentic worker：** 必须使用子 skill：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans，逐项任务实施本计划。各步骤使用复选框（<code>- [ ]</code>）语法跟踪。

**目标：** 基于同一份源代码，为 Intel macOS 和 Windows x64 发布 Desktop 0.4.3，包含用户批准的 Codex 风格发言刻度、对内置浏览器的结构化 agent 控制，以及可见、可撤销的多应用 Computer Use。

**架构：** 模型工具保留在 Harness 内，特权执行则置于 Electron 主进程和一个按需启动的原生 helper 中。两个带类型的服务 seam（<code>browserControl</code> 和 <code>computerControl</code>）通过 <code>desktop-control-protocol</code> 跨越唯一的父子 Node IPC 通道；该包是所有跨进程操作、结果和品牌化 ID DTO 的唯一真源。Electron 拥有租约、配额和策略，原生 helper 则镜像并重新校验它们。浏览器操作使用进程内 CDP wrapper 和绑定版本的不透明 ref；原生操作优先使用 AX/UIA ref，并且仅为支持视觉的模型使用坐标。

**技术栈：** TypeScript、React、CSS Modules、Cordis、Zod/Schemastery、Electron WebContentsView/CDP、Node 子进程 IPC、Rust、macOS AXUIElement/ScreenCaptureKit/CGEvent、Windows UI Automation/Windows Graphics Capture/SendInput、Vitest、Playwright 快照、electron-builder、GitHub Actions。

---

## 交付顺序

任务 1–2 是独立的发言刻度切片。任务 3–8 交付完整的浏览器控制切片。任务 9 以任务 3–6 为硬前置；任务 9–13 按观察 → 停止/清理验收 → 输入的顺序加入 Computer Use。只有前面的门禁全部为绿色后，任务 14 才会修改版本和发布元数据。

### 任务 1：让发言刻度具有明显的 Codex 风格

**文件：**
- 修改：<code>packages/client/ui-conversation/src/client/chat/PromptRail.tsx</code>
- 修改：<code>packages/client/ui-conversation/src/client/chat/ChatView.module.css</code>
- 修改：<code>packages/client/ui-conversation/src/client/locales.ts</code>
- 修改：<code>packages/client/ui-conversation/tests/chat-view.client.spec.tsx</code>
- 新建：<code>packages/client/ui-conversation/tests/prompt-rail-style.spec.ts</code>

- [ ] **步骤 1：先写失败测试，覆盖有效的主题 token、已批准的当前标记、悬停文本和最近锚点指针选择。**

~~~ts
expect(css).not.toContain('--dsw-alias-label-quaternary')
expect(css).toContain('var(--dsw-alias-label-tertiary,')
expect(nearestPromptIndex({ y: 260, top: 0, height: 520, count: 120 })).toBe(60)
expect(screen.getByRole('tooltip')).toHaveTextContent('第 12 条发言')
~~~

- [ ] **步骤 2：运行 <code>pnpm exec vitest run packages/client/ui-conversation/tests/chat-view.client.spec.tsx packages/client/ui-conversation/tests/prompt-rail-style.spec.ts</code>；确认因未定义的 token 和缺失的最近锚点 helper 进入 RED。**
- [ ] **步骤 3：用已定义的 token 和 fallback 替换无效颜色 token；实现已批准的 28–32 px 边栏、清晰可见的中性色刻度、DeepSeek 蓝色当前刻度和空心圆点、两行 tooltip，以及当前/总数计数。**
- [ ] **步骤 4：将指针命中测试移至刻度轨道。导出纯函数且进行范围限制的 <code>nearestPromptIndex</code>；保留刻度按钮的语义标签，但不让重叠的指针命中区域接管点击。**
- [ ] **步骤 5：重新运行聚焦测试；应当 PASS，且不存在编辑、fork、回退、删除、草稿或发送调用。**
- [ ] **步骤 6：提交 <code>fix(conversation): make prompt rail visible and precise</code>。**

### 任务 2：增加键盘和窄布局访问能力

**文件：**
- 修改：<code>packages/client/ui-conversation/src/client/chat/PromptRail.tsx</code>
- 修改：<code>packages/client/ui-conversation/src/client/chat/ChatView.module.css</code>
- 修改：<code>packages/client/ui-conversation/src/client/locales.ts</code>
- 修改：<code>packages/client/ui-conversation/tests/chat-view.client.spec.tsx</code>
- 新建：<code>apps/web/tests/prompt-rail.e2e.ts</code>
- 新建：<code>apps/web/tests/snapshots/</code> 下有意记录的浅色/深色发言刻度快照

- [ ] **步骤 1：先写失败测试，覆盖 roving tabindex、ArrowUp/ArrowDown/Home/End、Enter 激活、强制颜色下的可见性，以及 860 px 以下的紧凑型「发言导航」按钮。**
- [ ] **步骤 2：运行组件测试套件和回放快照测试；确认因刻度当前在 860 px 时消失且每个刻度都有相互重叠的按钮而进入 RED。**
- [ ] **步骤 3：实现单一键盘焦点拥有者，并为窄布局提供紧凑型弹出列表。保留精确的 <code>onActivate(seq)</code> 导航和减少动态效果行为。**
- [ ] **步骤 4：仅记录已批准的浅色/深色快照，然后运行 <code>pnpm run test:gui</code> 和 <code>DSH_SNAPSHOT=replay pnpm run test:web</code>。**
- [ ] **步骤 5：提交 <code>feat(conversation): keep prompt navigation accessible</code>。**

### 任务 3：定义封闭的 Desktop 控制 wire protocol

**文件：**
- 新建：<code>packages/control/desktop-control-protocol/package.json</code>
- 新建：<code>packages/control/desktop-control-protocol/tsconfig.json</code>
- 新建：<code>packages/control/desktop-control-protocol/src/brand.ts</code>
- 新建：<code>packages/control/desktop-control-protocol/src/bridge.ts</code>
- 新建：<code>packages/control/desktop-control-protocol/src/helper.ts</code>
- 新建：<code>packages/control/desktop-control-protocol/src/codec.ts</code>
- 新建：<code>packages/control/desktop-control-protocol/src/index.ts</code>
- 新建：<code>packages/control/desktop-control-protocol/src/invariant.ts</code>
- 新建：<code>packages/control/desktop-control-protocol/protocol-v1.json</code>
- 新建：<code>packages/control/desktop-control-protocol/tests/codec.spec.ts</code>
- 新建：共享的原始 <code>packages/control/desktop-control-protocol/fixtures/*.bin</code> frame 和一个极小 PNG。
- 新建：包 README 双语配对和配对记录。

- [ ] **步骤 1：先写失败的 codec/framing 测试，覆盖协议版本 1；全部 27 个 bridge kind；准确的内部 acquire/release 字段与结果矩阵；surface-kind/capability/target 约束；保持配对的 helper targets；总量与分类配额；协议版本、重复 JSON key 与严格未知字段拒绝；准确的 49,152 字节语义文本、65,536 字节 JSON payload、65,537 字节 JSON frame、4,194,304 字节 PNG body 和 4,194,321 字节 PNG frame 边界；分配前拒绝零值/超限长度前缀；拆分的长度 header/body；连续多帧；PNG 关联、哈希、尺寸、遗留与迟到；在 image-pending 状态前运行的两阶段 JSON 方向门禁；封闭类型与错误；不同品牌不可互换；冻结的嵌套输出；不可变 PNG 副本；以及被禁止的通用字段词汇。TypeScript 现在必须逐字节解码并重新编码共享原始 <code>.bin</code> fixture 与极小 PNG；任务 9 在 Rust 中执行完全相同的断言。**

~~~ts
export const BRIDGE_REQUEST_KINDS = [
  'control.lease.acquire', 'control.lease.release',
  'desktop.status',
  'browser.snapshot', 'browser.navigate', 'browser.click', 'browser.type',
  'browser.key', 'browser.select', 'browser.scroll', 'browser.wait',
  'browser.back', 'browser.forward', 'browser.reload', 'browser.stop',
  'computer.status', 'computer.list', 'computer.snapshot', 'computer.focus',
  'computer.click', 'computer.double-click', 'computer.drag', 'computer.type',
  'computer.key', 'computer.scroll', 'computer.wait', 'computer.stop',
] as const
export const HELPER_REQUEST_KINDS = [
  'status', 'list', 'snapshot', 'focus', 'click', 'double-click', 'drag',
  'type', 'key', 'scroll', 'wait', 'stop', 'lease.install', 'input.release',
] as const
export const CONTROL_KINDS = [
  'request.cancel', 'session.revoke', 'lease.revoke', 'parent.shutdown',
] as const
~~~

- [ ] **步骤 2：运行 <code>pnpm exec vitest run packages/control/desktop-control-protocol/tests/codec.spec.ts</code>；确认因包不存在而进入 RED。**
- [ ] **步骤 3：让本包成为所有跨边界操作、结果和品牌 DTO 的唯一真源。提交机器可读的 <code>protocol-v1.json</code>，一次冻结完整的 bridge/helper/control 清单（其中包含全部 27 项 bridge 请求）、封闭字段矩阵和所有数值上限；本任务现在根据它生成或验证 TypeScript，并要求任务 9 的 Rust 消费同一 manifest 和 fixture，同时拒绝任何通用 schema escape。TypeScript 必须逐字节解码并重新编码所有共享原始 <code>.bin</code>/极小 PNG fixture。定义品牌化 <code>RequestId</code>、<code>ControlLeaseId</code>、<code>BrowserRef</code>、<code>ComputerRef</code> 和 <code>PngTransferId</code>；导入并重新导出官方 <code>SessionId</code>；携带 <code>leaseRevision</code>。顶层 <code>messageKind</code> 准确限定为 <code>request</code>、<code>response</code> 或 <code>control</code>；响应准确限定为 <code>ok</code> 或 <code>error</code> 并回显 <code>requestKind</code>；错误只使用 <code>NOT_SUPPORTED</code>、<code>UNAUTHORIZED</code>、<code>LEASE_EXPIRED</code>、<code>LEASE_REVOKED</code>、<code>STALE_REF</code>、<code>TARGET_CLOSED</code>、<code>PERMISSION_DENIED</code>、<code>POLICY_DENIED</code>、<code>DUPLICATE_REQUEST</code>、<code>TOO_MANY_PENDING</code>、<code>QUOTA_EXCEEDED</code>、<code>BINARY_MISMATCH</code>、<code>BUSY</code>、<code>TIMEOUT</code>、<code>CANCELLED</code>、<code>DISCONNECTED</code> 和 <code>INTERNAL</code>，其 UTF-8 消息不超过 512 字节，<code>retryable</code> 为布尔值。**
- [ ] **步骤 4：实现严格的不可变解码和统一二进制 envelope。Rust 四字节大端长度包括 tag 与 body；在分配前拒绝零值或超过 4,194,321 的值。公开两阶段 decode/correlate seam（或等价的方向 predicate），使任务 5 可以在 correlator 记录 image metadata 并等待 PNG 前，对已验证 JSON 消息执行方向检查。每个 bridge 请求都携带官方 session/request/deadline 基础字段。增加仅供内部使用的 <code>control.lease.acquire</code> 字段：一个封闭的 <code>surfaceKind</code>、不得重复的封闭 <code>capabilities</code> 数组，以及保持配对的 <code>targets: { appId, windowIds[] }[]</code>。浏览器 targets 为空；原生 targets 每个应用至少包含一个当前窗口，并满足 manifest 中应用/窗口/列表/标识符/JSON 上限。其不可变结果只包含 lease ID/revision、surface kind、生效后的 targets/capabilities 和空闲/硬相对时长。增加携带 lease ID/revision 的 <code>control.lease.release</code> 与准确的 <code>{ released: true }</code> 结果。二者都不接受或返回配额、时钟、<code>approved</code> 或 digest；模型 DTO 不能发出它们。helper 请求把 Electron 缩短后的时长作为 <code>timeoutMs</code> 携带；lease 作用域请求增加 <code>leaseId</code>/<code>leaseRevision</code>，只有目标作用域请求增加 <code>appId</code>/<code>windowId</code>/<code>snapshotRevision</code>。<code>lease.install</code> 携带保持配对的 targets，以及由 Electron 编写的配额：总量 <code>operations</code> 加 snapshot/pointer/key/text 分类；<code>agentId</code> 只用于显示。<code>input.release</code> 没有 lease 字段、无需批准且仅限 Electron，不能由子进程/模型 DTO 发出。拒绝通用 <code>command</code>、<code>script</code>、<code>selector</code>、<code>javascript</code>、<code>method</code>、<code>args</code>、<code>env</code>、<code>cwd</code>、<code>path</code>、<code>channel</code>、<code>ipc</code>、<code>payload: unknown</code>、<code>data: unknown</code>、<code>Record</code> 和索引签名。畸形、未知、版本错误、超限或顺序错误的输入只关闭控制通道。方向白名单、重复请求 ID、迟到响应、取消、pending 上限、tombstone 与子进程 generation 等有状态语义属于任务 5，不属于本 codec。**
- [ ] **步骤 5：提交 <code>feat(control): define closed desktop protocol</code>。**

### 任务 4：定义 Browser Control 和 Computer Control 服务

**文件：**
- 新建：<code>packages/control/browser-control/</code>，包含 <code>src/index.ts</code>、<code>src/types.ts</code>、<code>src/invariant.ts</code>、测试、manifest、tsconfig 和 README 配对。
- 新建：<code>packages/control/computer-control/</code>，包含 <code>src/index.ts</code>、<code>src/types.ts</code>、<code>src/policy.ts</code>、<code>src/invariant.ts</code>、测试、manifest、tsconfig 和 README 配对。

- [ ] **步骤 1：先写失败的 Service 测试，覆盖稳定注册、拒绝重复 provider、内部 acquire、共享 client/cache 行为、模型 schema 准确排除权威字段、会话撤销、过期 ref、操作限制、安全字段、敏感操作分类，以及 metadata 与 PNG 必须同现或同缺的类型化不可变 snapshot envelope。验证 transfer ID、字节长度、SHA-256 与尺寸保持关联，并且每次读取 PNG 都返回副本。**

~~~ts ignore-check
export abstract class BrowserControl extends Service {
  abstract acquireLease(request: ControlLeaseAcquireRequest, signal: AbortSignal): Promise<ControlLeaseAcquireResult>
  abstract snapshot(request: BrowserSnapshotRequest, signal: AbortSignal): Promise<BrowserSnapshotEnvelope>
  abstract act(request: BrowserActionRequest, signal: AbortSignal): Promise<BrowserActionResult>
  abstract revokeSession(sessionId: SessionId): Promise<void>
}

export interface BrowserSnapshotEnvelope {
  readonly result: BrowserSnapshotResult
  readonly png?: ImmutablePng
}
~~~

~~~ts ignore-check
export abstract class ComputerControl extends Service {
  abstract acquireLease(request: ControlLeaseAcquireRequest, signal: AbortSignal): Promise<ControlLeaseAcquireResult>
  abstract status(): Promise<ComputerControlStatus>
  abstract list(request: ComputerListRequest, signal: AbortSignal): Promise<ComputerListResult>
  abstract snapshot(request: ComputerSnapshotRequest, signal: AbortSignal): Promise<ComputerSnapshotEnvelope>
  abstract act(request: ComputerActionRequest, signal: AbortSignal): Promise<ComputerActionResult>
  abstract stop(sessionId: SessionId): Promise<void>
}

export interface ComputerSnapshotEnvelope {
  readonly result: ComputerSnapshotResult
  readonly png?: ImmutablePng
}
~~~

- [ ] **步骤 2：运行两个包的测试套件；在实现服务之前确认进入 RED。**
- [ ] **步骤 3：复用或重新导出所有协议操作、结果、lease-acquire、品牌、PNG metadata 与 <code>ImmutablePng</code> 类型；不得在任一服务包中复制其声明。两个 seam 都公开内部 <code>acquireLease</code>，复用协议 DTO/result。它们的 Host provider 共享唯一进程级 IPC client 与 lease cache，在内部取得官方 session 和 transport 字段、先 acquire 再执行普通操作，并且绝不把 acquire、session ID、lease ID/revision、审批、配额、时钟或 digest 字段投影到模型工具 schema 中。不得新增第三个 Service。增加不可变、有界且仅属于服务的 snapshot envelope 与 ref 类型，将浏览器 ref 绑定到 surface + revision，并将计算机 ref 绑定到 app + process + window + revision + scale。snapshot envelope 暴露协议 metadata 和封装的 <code>ImmutablePng</code>，绝不暴露可变原始字节，并强制 <code>result.image === undefined</code> 当且仅当 <code>png === undefined</code>。**
- [ ] **步骤 4：实现策略结果 <code>ALLOW</code>、<code>APPROVAL_REQUIRED</code> 和 <code>DENY</code>。已知 secure text、OTP、付款、文件、生物识别、密码管理器和操作系统隐私/安全目标始终返回 DENY；无法确定敏感性时按 fail-closed 处理。**
- [ ] **步骤 5：重新运行测试并提交 <code>feat(control): add browser and computer seams</code>。**

### 任务 5：将 Harness 子进程桥接到 Electron 主进程

**文件：**
- 新建：<code>packages/control/desktop-control-host/package.json</code>、<code>tsconfig.json</code>、<code>src/ipc-client.ts</code>、<code>src/browser-provider.ts</code>、<code>src/computer-provider.ts</code>、<code>src/index.ts</code>、<code>src/invariant.ts</code>、聚焦测试和 README 双语配对/记录。
- 新建：<code>apps/desktop/src/control/bridge-server.ts</code> 与 <code>apps/desktop/tests/control-bridge.spec.ts</code>。
- 新建：<code>apps/desktop/tests/fixtures/harness-ipc-child.mjs</code>，作为真实 spawn 的子进程 IPC fixture。
- 修改：<code>apps/desktop/src/harness/process.ts</code>、<code>apps/desktop/tests/process.spec.ts</code>、<code>apps/desktop/src/main.ts</code> 和 <code>apps/desktop/tsdown.config.ts</code>。
- 修改：<code>apps/desktop/desktop.cordis.patch.yml</code>、<code>apps/desktop/package.json</code>、<code>apps/desktop/tsconfig.json</code> 和 <code>apps/desktop/tests/manifest.spec.ts</code>。
- 修改：<code>scripts/stage-desktop.ts</code>、<code>scripts/stage-desktop.spec.ts</code>、<code>scripts/verify-package-readme-model-experience.ts</code>、<code>tsconfig.host.json</code> 和 <code>pnpm-lock.yaml</code>。
- 修改：<code>packages/control/README.md</code>、对应中文文档/配对记录，以及生成的 module graph 双语配对/记录。

- [ ] **步骤 1：先为包和 Desktop 编写失败测试，覆盖：两个 provider 共享唯一的进程级 client 与 lease cache；两个内部 lease 请求；在 image correlation 前检查准确链路/方向白名单；方向错误且带 image 的 response 即使没有后续 PNG 也立即关闭控制通道；advanced serialization 下每条消息恰好一个已复制且没有长度前缀的原始 <code>Uint8Array</code> frame；不可交错的 JSON 加可选 PNG envelope；请求关联；严格的 <code>now < deadline <= now + 30,000</code> 边界和合法缩短；重复 ID；取消；可等待的 <code>agent/turn-stopping</code> release 加 <code>turn/end</code>/<code>session/flush</code> fallback tail；session 与准确 lease 撤销；超时；迟到 response；准确的 32 个 pending request 上限；准确的 256 个终态 tombstone 上限；子进程 generation；断连拒绝；listener 清理；以及协议内容零日志。response 必须同时匹配 pending 的 <code>requestId</code> 和 <code>requestKind</code>；取消还必须匹配其官方 <code>sessionId</code>；lease 撤销必须准确匹配从已验证 pending request 派生的 session、lease ID 与 lease revision。**
- [ ] **步骤 2：运行 Host 包、bridge、process、manifest 和 staging 测试；确认此时 Harness 只有 stdin/stdout/stderr、Desktop patch 没有 Host provider 且 stage 缺少其运行时文件，因此进入 RED。**
- [ ] **步骤 3：保留 <code>spawn()</code>，将准确拥有的子进程改为 <code>stdio: ['ignore', 'pipe', 'pipe', 'ipc']</code> 并设置 <code>serialization: 'advanced'</code>。复制环境并显式剔除继承的 <code>NODE_CHANNEL_FD</code> 与 <code>NODE_CHANNEL_SERIALIZATION_MODE</code>，再由 Node 只根据 <code>ipc</code> stdio 项创建 fd 3 和内部通道管线；绝不设置这两个变量、手动打开 fd 3 或改用 <code>fork()</code>。仅在 <code>HarnessProcess</code> 准确拥有的子进程上公开带类型的 send/message/disconnect hook。在 <code>HarnessProcess.stop()</code> 内增加可等待的 <code>beforeStop</code> hook，使其自身的启动失败清理以及所有 retry/quit 调用都通过同一控制关闭路径。用真实 fixture 证明双向交换的是原始 <code>Uint8Array</code> frame，Node 链路绝不增加 helper 链路使用的四字节长度前缀。不得增加回环端口、远程调试端口、renderer 中继或环境 bearer token。**

~~~ts ignore-check
const childEnv = { ...process.env }
delete childEnv.NODE_CHANNEL_FD
delete childEnv.NODE_CHANNEL_SERIALIZATION_MODE
childEnv.ELECTRON_RUN_AS_NODE = '1'

const child = spawn(executable, args, {
  cwd: workspace,
  detached: platform !== 'win32',
  env: childEnv,
  stdio: ['ignore', 'pipe', 'pipe', 'ipc'],
  serialization: 'advanced',
})
~~~

- [ ] **步骤 4：在严格 JSON 解码后、dispatch 或进入 image-correlation pending 状态前检查方向。Harness → Electron 接受全部 27 个 <code>BRIDGE_REQUEST_KINDS</code>，包括仅供内部使用的 <code>control.lease.acquire</code>/<code>control.lease.release</code>；control 只接受同 session/request 的 <code>request.cancel</code> 与 session dispose 时的 <code>session.revoke</code>；拒绝所有 response、PNG、helper 专用 request、<code>lease.revoke</code> 和 <code>parent.shutdown</code>。Electron → Harness 只接受对应的封闭 bridge response、其紧邻截图 PNG、<code>lease.revoke</code> 和 <code>parent.shutdown</code>；拒绝所有 request、<code>request.cancel</code> 和 <code>session.revoke</code>。方向错误且带 image 的 response 不等待其声明的 PNG，立即关闭控制通道。未知、畸形、孤立 PNG 或方向错误的输入只关闭这条控制链路，Harness/Web/chat 继续运行。为任务 9 保持 helper 方向冻结：Electron → helper 允许全部 14 个 helper request 和四种由 Electron 发起的高优先级 control；helper → Electron 只允许匹配的 helper response 与紧邻截图 PNG。**
- [ ] **步骤 5：实现最多 32 条 pending 记录的 ledger，并使用按插入顺序淘汰、容量准确为 256 个终态 request ID 的 FIFO tombstone 集合。每条记录绑定 <code>{ requestId, requestKind, sessionId, generation, deadline, cancellation, lease? }</code>，其中可选 <code>lease: { leaseId, leaseRevision }</code> 只从已验证的 lease-bearing request 派生；拒绝与 live 或 tombstone ID 重复的请求；只结算 ID/kind 完全匹配的 response；取消必须匹配 session；丢弃所有迟到终态 response。只读取一次 <code>nowUnixMs</code>，并且只接受 <code>nowUnixMs < deadlineUnixMs <= nowUnixMs + 30,000</code>；绝不把无效 deadline clamp 成合法，只把缩短后的正数剩余时间转换为 <code>timeoutMs</code>。准确的 <code>lease.revoke</code> 先同步让匹配共享 cache 失效，再把 session/lease ID/revision 全部相同的记录以 <code>LEASE_REVOKED</code> 只结算一次并只生成一个 tombstone；其他 session、lease ID、revision 和无 lease 工作不受影响。重复 revoke 保持幂等且不刷新 FIFO 顺序；迟到响应直接丢弃。abort、timeout、disconnect、<code>parent.shutdown</code> 和 dispose 同样都必须且只能结算一次受影响的 pending 工作。**
- [ ] **步骤 6：每一端都通过一个 send-callback 队列串行发送完整 envelope。每个输入 frame 入队前都必须复制；JSON frame 及其可选关联 PNG 发送期间不能插入其他 envelope，并且只能由 send callback 推进队列。入站处理、异步 provider completion、timer/abort continuation 与 send callback 都必须同时检查准确子进程身份与 generation。新 listener 激活前先推进 generation；旧 completion 只能丢弃其隔离副本。进程级 <code>disconnect</code> 会清理控制队列/listener，并以 <code>DISCONNECTED</code> 拒绝 pending 工作，但绝不能终止 Harness 或影响聊天。**
- [ ] **步骤 7：在不可变的 Desktop 专用 Cordis patch 中只挂载一个 <code>desktop-control-host</code> 实例。其唯一 IPC client 与 lease cache 同时支撑 Browser Control 和 Computer Control provider；绝不能为每个 provider 分别创建 listener、ledger 或权威 cache。provider 在内部取得官方 session/transport 字段，不把 lease 请求注册为工具。正常路径由 <code>agent/turn-stopping</code> 同步让 cache 失效并等待有界的 <code>control.lease.release</code> tail。由于 <code>session/event</code> 是提交后的 fire-and-forget，且错误/取消路径可能跳过该可等待事件，其 <code>turn/end</code> observer 只能同步失效并排入幂等 fallback tail，不能承诺 release 先于 <code>turn/end</code> commit。可等待的 <code>session/flush</code> listener 在下一个受 checkpoint 保护的模型/顶层工具副作用前 drain 该 tail。<code>session/disposed</code> 让 cache 失效并排入 <code>session.revoke</code>；插件 effect disposer 在移除 listener 前 drain 全部 release/revoke tail。transport 失败时 cleanup listener 仍需完成，并让控制不可用而不是破坏普通聊天。**

- [ ] **步骤 8：把本包加入 Desktop dependency 与项目引用闭包、Host 聚合、lockfile、包组文档/模型体验分类、生成的 module graph、staging 必需文件白名单、manifest 断言和 stage 测试。把 <code>@deepseek-ai/dsh-desktop-control-protocol</code> 保留在 Desktop main 的 <code>neverBundle</code> 列表中。删除或构建 stage 前先验证唯一 canonical Host patch row。stage 必须包含 protocol 的 <code>package.json</code>、<code>lib/index.js</code>、<code>lib/invariant.js</code>、<code>protocol-v1.json</code>，Host 的 <code>package.json</code>、<code>lib/index.js</code>、<code>lib/invariant.js</code>，以及 Browser/Computer Service 各自的 <code>package.json</code>、<code>lib/index.js</code>、<code>lib/invariant.js</code>。从 stage-shaped 临时目录导入构建后的 Desktop main，证明协议 manifest 能被解析。控制链路缺失只会让 provider 不可用，不会破坏普通 Harness 启动。**
- [ ] **步骤 9：lease 权威与原生输入释放仍由任务 6 负责。在 Electron 原生 challenge 和任何必要的 helper <code>lease.install</code> 确认成功前，不得返回 acquire response。release response 只有在撤销、清空队列、下游浏览器/helper 清理与已知输入释放确认完成后才返回 <code>released: true</code>。正常关闭时，先停止接收新工作并等待同一套任务 6 清理。随后由可等待的 <code>HarnessProcess.beforeStop</code> hook 入队 <code>parent.shutdown</code>，在有界时间内等待其 callback，断开准确的 IPC 链路并移除 listener，之后才终止进程树。<code>HarnessProcess.start()</code> 内部的启动失败清理、retry 和 quit 都必须经过同一 hook。transport 日志只允许方向、generation、有界计数和关闭原因码，绝不能包含 frame 字节、已解码 request/result、语义/页面/字段文本、PNG、Cookie、token 或绝对用户路径。**
- [ ] **步骤 10：重新运行聚焦测试以及 Host/Desktop typecheck、包/约束/publint gate、翻译配对、生成文档新鲜度、准确 staging 检查、stage-shaped built-main import 和真实 spawn fixture。全部通过后才提交 <code>feat(desktop): bridge control over owned child ipc</code>。**

### 任务 6：增加权威 ControlLease 和敏感操作策略

**文件：**
- 新建：<code>apps/desktop/src/control/control-lease.ts</code>
- 新建：<code>apps/desktop/src/control/control-coordinator.ts</code>
- 新建：<code>apps/desktop/src/control/native-approval.ts</code>
- 新建：<code>apps/desktop/src/control/action-grant.ts</code>
- 新建：<code>apps/desktop/src/control/policy.ts</code>
- 新建：<code>apps/desktop/src/control/settings-store.ts</code>
- 新建：<code>apps/desktop/src/control/audit.ts</code>
- 新建：<code>apps/desktop/src/control/emergency-shortcut.ts</code>
- 新建：<code>apps/desktop/tests/control-lease.spec.ts</code>
- 新建：<code>apps/desktop/tests/control-coordinator.spec.ts</code>
- 新建：<code>apps/desktop/tests/control-native-approval.spec.ts</code>
- 新建：<code>apps/desktop/tests/control-action-grant.spec.ts</code>
- 新建：<code>apps/desktop/tests/control-security.spec.ts</code>
- 新建：<code>apps/desktop/tests/control-settings-store.spec.ts</code>
- 新建：<code>apps/desktop/tests/control-audit.spec.ts</code>
- 新建：<code>apps/desktop/tests/control-emergency-shortcut.spec.ts</code>
- 修改：<code>apps/desktop/src/application.ts</code>
- 修改：<code>apps/desktop/src/main.ts</code>
- 修改：<code>apps/desktop/tests/main-lifecycle.spec.ts</code>

- [ ] **步骤 1：先写使用单调假时钟的失败测试，覆盖：进程内单一活跃 lease；官方 session/保持配对的 target/capability 绑定；Electron 编写的总量与分类配额；安全整数单调递增且溢出拒绝的 <code>leaseRevision</code>；准确的空闲 <code>>=</code> 五分钟与硬时长 <code>>=</code> 二十分钟到期；绑定 generation 的 timer；以及等于 <code>min(合法且为正数的 bridge 剩余时间, 30s, hard 剩余时间)</code> 的有效 helper timeout。只有已接受的 lease 作用域动作刷新 idle，hard 到期永不后移。覆盖任务完成；正常路径在 <code>agent/turn-stopping</code> 可等待释放；错误/取消路径由提交后的 <code>turn/end</code> 排入 fallback 并由 <code>session/flush</code> drain；会话切换/dispose 与 disposer drain；app 重启；用户 Stop；disconnect；close-to-tray；quit；retry；启动失败；helper crash；以及非当前会话拒绝。fallback 路径不得声称 release 先于 <code>turn/end</code> commit。证明子进程/模型数据不能携带 <code>approved</code>、签发 lease、提议配额、推进其 revision、延长 deadline/lease 时钟，也不能把已允许应用与另一应用的窗口交叉组合。**

~~~ts ignore-check
interface ControlLease {
  id: ControlLeaseId
  sessionId: SessionId
  leaseRevision: number
  agentId: string
  surfaceKind: 'browser-ephemeral' | 'browser-human-persistent' | 'native-application'
  targets: readonly { readonly appId: string; readonly windowIds: readonly string[] }[]
  capabilities: ReadonlySet<'observe' | 'pointer' | 'keyboard'>
  quotas: ControlLeaseQuotaSnapshot
  issuedAt: number
  lastActionAt: number
  hardExpiresAt: number
}
~~~

- [ ] **步骤 2：运行租约/安全测试；确认因不存在权威方而进入 RED。**
- [ ] **步骤 3：实现一个由 Electron 主进程拥有的权威 coordinator。它把请求中的期望子集与当前枚举、普通 app allowlist 和固定策略取交集，然后在全局只拥有一个 pending 原生 dialog 和最多一个进程级活跃 lease。owner 窗口缺失或不可见、Escape、关闭、abort、异常或并发请求都会拒绝。迟到批准重新校验官方 session、target、revision 和 allowlist。Harness <code>ctx.approval</code> 可以请求交互并提供 UX，但不能授权任何事项。同一个 Harness Node 进程内已安装的 Host 插件属于可信计算基；只承诺 renderer/page/model 数据不能伪造 Electron 确认。renderer 的“交给 Agent”操作只创建由主进程拥有、供下一个官方子进程 acquire 使用的 intent，不能签发 lease 或携带 session 权威。**
- [ ] **步骤 4：通过 <code>lease.install</code> 安装由 Electron 编写、保持 targets 配对的 lease 快照，并在 helper 确认成功前不返回 descriptor。每个 lease 作用域请求在 dispatch 前先扣一次总量 <code>operations</code>，然后在每次适用的原生调用前扣减既有 snapshot/pointer/key/text 分类。<code>agentId</code> 只用于显示。让 <code>lease.revoke</code> 保持幂等、无需批准且优先级高于操作队列。只有可等待的撤销 → 清空队列 → 浏览器/helper 清理 → 释放已知输入 → 释放确认全部完成后，才返回 <code>released: true</code>。完整 helper 关闭随后继续执行 stdin EOF → 等待 helper → 有界 terminate/kill ladder。**
- [ ] **步骤 5：为持久化人工浏览器的每个 mutation 实现一次性 action grant：<code>navigate</code>、<code>click</code>、<code>type</code>、<code>key</code>、<code>select</code>、<code>scroll</code>、<code>back</code>、<code>forward</code> 和 <code>reload</code>。Electron 计算准确动作 digest、显示并验证其原生 challenge、只消费一次 grant，并在 dispatch 前立即重新检查 session/target/revision。page/model/renderer/child 不能提交或消费 digest 或 grant。Stop 永远无需批准。**
- [ ] **步骤 6：通过独立 strict schema 和原子文件替换持久化设置。只存储普通 app allowlist 条目、普通开关与 accelerator；绝不存储窗口 ID、lease、ref、digest、截图或 session 权威。轮换审计行，且每行只包含时间、加盐 session hash、opaque app ID、动作类别与结果；绝不包含页面/字段文字、URL/query、路径、截图、Cookie、token 或秘密。**
- [ ] **步骤 7：只在 lease 活跃时注册紧急 accelerator，注册失败时让授权 fail closed。原子重绑，只注销本功能拥有的 accelerator，绝不调用 <code>unregisterAll</code>。callback 先同步撤销权威并关闭 admission，再等待清理。活跃控制清理完成前，禁止 close-to-tray 隐藏窗口。让 <code>DesktopApplication</code> 在会话切换/dispose、disconnect、quit、retry 和 helper crash 时先等待控制清理，再停止 Harness；注明整机断电或同时终止 Electron/helper 无法保证释放。**
- [ ] **步骤 8：重新运行聚焦测试，覆盖单调时钟准确边界、总量配额、dialog pending/race、快捷键失败/重绑、renderer 伪造、<code>ctx.approval</code> 不具权威、helper-install 失败、持久浏览器 action grant 和每个可等待生命周期路径，并运行 Desktop typecheck 与仓库门禁。提交 <code>feat(desktop): enforce control leases and policy</code>。**

### 任务 7：实现语义浏览器适配器

**文件：**
- 新建：<code>apps/desktop/src/browser/cdp.ts</code>
- 新建：<code>apps/desktop/src/browser/agent-controller.ts</code>
- 新建：<code>apps/desktop/src/browser/surface-manager.ts</code>
- 新建：<code>apps/desktop/src/browser/policy.ts</code>
- 新建：<code>apps/desktop/tests/browser-agent.spec.ts</code>
- 新建：<code>apps/desktop/tests/browser-policy.spec.ts</code>
- 修改：<code>apps/desktop/src/browser/contracts.ts</code>
- 修改：<code>apps/desktop/src/browser/controller.ts</code>
- 修改：<code>apps/desktop/tests/browser-contracts.spec.ts</code>

- [ ] **步骤 1：使用假的 <code>webContents.debugger</code> 编写失败测试，覆盖其他 debugger 占用时返回 <code>BUSY</code> 且不 detach、己方 detach、主文档/same-document 导航 race、迟到 CDP 响应、有界 Accessibility 遍历、secure/file 拒绝、陈旧 mount token、跨会话 <code>BUSY</code>、带 redirect 的 URL SSRF 拒绝、唯一临时 partition 清除，以及仅 ref 的点击/输入/按键/选择/滚动/等待。**
- [ ] **步骤 2：运行浏览器测试；确认因 Agent API 缺失且当前使用持久化 partition 而进入 RED。**
- [ ] **步骤 3：只使用进程内 CDP wrapper。从 <code>Accessibility.getRootAXNode</code> 开始，以有界广度优先方式遍历 <code>Accessibility.getChildAXNodes</code>；只允许用只读 <code>DOM.describeNode</code> 判定 <code>type</code>/<code>autocomplete</code>/<code>disabled</code>/<code>readonly</code>，并允许 <code>DOM.getBoxModel</code>、固定 Input 方法和 <code>Page.captureScreenshot</code>。绝不使用或公开 <code>Runtime</code>、JavaScript 求值、selector、任意 CDP 方法或 debugger 访问。默认省略可编辑值，无法确定敏感性时拒绝。**
- [ ] **步骤 4：实现一个由 Electron 主进程拥有的 <code>BrowserSurfaceManager</code>，包含全局排他的 Agent surface/lease、surface generation 和 mount token。第二个会话返回 <code>BUSY</code>；陈旧 hide 回调不能影响替代实例。Agent 页面可见并使用唯一非持久化 partition。人工登录 partition 保持持久化，只在“交给 Agent”后转移其当前可见实例。跟踪 <code>attachedByUs</code>；遇到其他 debugger 时返回 <code>BUSY</code>，绝不 detach。Stop 等待 detach、视图清理、partition 清除和撤销完成。**
- [ ] **步骤 5：将遍历限制为 2,000 个原始节点、32 层深度、512 次 CDP 调用、2,000 ms 墙上时间、300 个可操作输出节点、49,152 个 UTF-8 语义字节、65,536 字节编码 JSON payload 和 4,194,304 个 PNG 字节。detach、销毁、主文档导航、same-document 导航和语义树大改会使 ref 与迟到响应失效。浏览器操作只接受 ref，不存在浏览器坐标操作。<code>browser.wait</code> 只允许 <code>duration</code>、<code>navigation</code> 或 <code>loading-idle</code>，duration 不超过 10,000 ms。拒绝带 userinfo 的 URL，以及每个初始或 redirect 的 loopback/link-local/私网目标，用户明确加入 allowlist 时除外。拒绝 file input、upload 和 chooser。**
- [ ] **步骤 6：重新运行测试并提交 <code>feat(desktop): add semantic browser adapter</code>。**

### 任务 8：公开 Browser Control 工具和可见接管 UI

**文件：**
- 新建：<code>packages/control/tool-browser-control/</code>，包含 snapshot/action/presentation 模块、测试、manifest、tsconfig 和 README 配对。
- 修改：<code>packages/extensions/desktop-workbench/src/client/BrowserMode.tsx</code>
- 修改：<code>packages/extensions/desktop-workbench/src/client/BrowserMode.module.css</code>
- 修改：<code>packages/extensions/desktop-workbench/src/client/locales.ts</code>
- 修改：<code>packages/extensions/desktop-workbench/src/client/index.tsx</code>
- 修改：<code>packages/extensions/desktop-workbench/src/client/preferences.ts</code>
- 修改：<code>packages/extensions/desktop-workbench/package.json</code> 和 client/host tsconfig。
- 新建：<code>packages/extensions/desktop-workbench/tests/browser-mode.client.spec.tsx</code>
- 修改：<code>apps/desktop/src/preload-api.ts</code>
- 修改：<code>apps/desktop/src/preload.ts</code>
- 修改：<code>apps/desktop/src/preferences.ts</code>
- 修改：<code>apps/desktop/package.json</code> 和 <code>apps/desktop/tsconfig.json</code>
- 修改：<code>apps/cli/package.json</code> 和 <code>apps/cli/tsconfig.json</code>
- 修改：<code>apps/desktop/tests/preload-api.spec.ts</code>
- 修改：<code>apps/desktop/desktop.cordis.patch.yml</code>
- 修改：<code>pnpm-lock.yaml</code> 和生成的 module-graph 双语配对/记录。
- 修改：适当的非最小 Desktop agent preset 及其 e2e 测试。

- [ ] **步骤 1：先为独立的封闭工具编写失败测试，覆盖 <code>browser_snapshot</code>、<code>browser_navigate</code>、<code>browser_click</code>、<code>browser_type</code>、<code>browser_key</code>、<code>browser_select</code>、<code>browser_scroll</code>、<code>browser_wait</code>、<code>browser_back</code>、<code>browser_forward</code>、<code>browser_reload</code> 和 <code>browser_stop</code>。证明 wait 模式与 duration 封闭且有界，任何工具都不接受坐标、文件、upload 或 chooser handle。**
- [ ] **步骤 2：断言所有浏览器模型都只能使用 ref；支持视觉的模型可以通过 <code>ctx.attachments.saveImage()</code> 保存有界 PNG 字节并返回 ImageBlock，但截图永远不会启用坐标操作。**
- [ ] **步骤 3：拒绝已知密码/OTP/付款/文件目标。记录页面 JavaScript 可以让普通操作产生外部影响，AX 无法分类每种结果。临时 surface 任务 lease 只覆盖固定普通清单。在持久化人工 surface 上，每个变更型 <code>navigate</code>/<code>click</code>/<code>type</code>/<code>key</code>/<code>select</code>/<code>scroll</code>/<code>back</code>/<code>forward</code>/<code>reload</code> 都分别要求绑定准确动作 digest 的一次性 Electron 原生 challenge。<code>ctx.approval.request()</code> 可以渲染 UX，但不是授权；工具 schema 绝不接受 <code>approved</code>，页面/模型/renderer/子进程不能提供、消费或复用 digest 或 grant。Stop 永远无需批准。**
- [ ] **步骤 4：在浏览器工具栏旁增加“交给 Agent/停止接管”控件。在转移当前可见的持久化人工实例前显示登录态警告。“交给 Agent”只提交保存在 Electron 主进程中的用户 intent，让下一个官方子进程 acquire 可以被确认；renderer/preload 不能签发 lease、选择 session 权威或公开模型操作原语。Stop 在 manager 清理完成前保持 pending。**
- [ ] **步骤 5：只有 provider 存在时才条件注册工具，保证普通 CLI/Web 在没有 provider 时启动行为不变。运行聚焦的工具、preload、workbench UI、preset、GUI、其他 debugger 占用/detach/导航 race/mount token/SSRF/迟到 CDP/partition 清除、无 provider CLI/Web 和 fixture 浏览器循环测试。在本地完成 <code>navigate → snapshot → click → type → wait → back → stop</code>。**
- [ ] **步骤 6：提交 <code>feat(browser): add visible agent control</code>。**

### 任务 9：构建不含输入注入的按需 Rust helper

**文件：**
- 新建：<code>native/computer-use-helper/.gitignore</code>、workspace <code>Cargo.toml</code>、已提交的 <code>Cargo.lock</code> 和准确锁定版本的 <code>rust-toolchain.toml</code>。
- 新建：<code>native/computer-use-helper/crates/</code> 下三个 crate：协议、平台无关 core 和 helper 二进制。
- 新建：<code>scripts/build-computer-use-helper.ts</code> 及其 Vitest 文件。
- 修改：<code>package.json</code>、<code>scripts/stage-desktop.ts</code>、<code>scripts/stage-desktop.spec.ts</code> 和 <code>apps/desktop/electron-builder.yml</code>。
- 新建：<code>apps/desktop/src/control/helper-process.ts</code> 及测试。
- 修改：<code>.github/workflows/desktop-release.yml</code>、<code>.github/workflows/windows-desktop.yml</code> 和 <code>THIRD_PARTY_NOTICES.md</code>。

- [ ] **步骤 1：任务 3–6 通过前不得开始。增加 Rust 测试，验证已提交的 v1 manifest，并重新编码与 TypeScript 完全相同的原始 <code>.bin</code>/极小 PNG fixture 字节。拒绝重复 JSON key、未知字段、错误版本、分配前的零值/超限前缀、畸形 ref，以及无效的 JSON/PNG 关联、哈希、尺寸、顺序、遗留或迟到；覆盖拆分 header/body 与连续多帧。**
- [ ] **步骤 2：先写失败的 TS 测试，覆盖一个按需子进程、有界 frame I/O、立即将 <code>timeoutMs</code> 转换为单调时钟、不可变 PNG 副本、崩溃映射、stdin EOF 清理，以及空闲时 helper 进程数为零。在构建脚本、staging 和 packaged smoke 中分别独立验证 Mach-O/PE 架构。**
- [ ] **步骤 3：在三个 crate 的 workspace 中实现 <code>serde(deny_unknown_fields)</code>。只读切片只执行 <code>status</code>、<code>list</code>、<code>snapshot</code>、<code>stop</code>、<code>lease.install</code>、幂等高优先级 <code>lease.revoke</code> 和清理。它解码保留的 Electron 专用 <code>input.release</code> 与已冻结输入清单，但在原生输入启用前返回 <code>NOT_SUPPORTED</code>。lease 空闲/硬到期只使用单调时钟。**
- [ ] **步骤 4：构建到准确的已忽略平台路径 <code>apps/desktop/native-bin/darwin-x64/computer-use-helper</code> 或 <code>apps/desktop/native-bin/win32-x64/computer-use-helper.exe</code>。通过 electron-builder <code>extraResources</code> 只 stage 一个匹配二进制文件；缺失、架构错误、不可执行或存在额外平台产物时均按 fail-closed 方式失败。**
- [ ] **步骤 5：在 Mac/Windows 工作流中锁定并显式安装 Rust toolchain；运行 <code>cargo build --locked</code>、fmt、以警告拒绝模式运行 clippy、Cargo 测试、构建脚本测试、stage 测试、全部三层架构检查、app 主进程构建和第三方通知验证。**
- [ ] **步骤 6：提交 <code>feat(computer): add read-only native helper</code>。**

### 任务 10：实现 macOS 观察和权限

**文件：**
- 在 <code>native/computer-use-helper/crates/helper/src/platform/macos/</code> 下新建用于辅助功能、捕获、权限、缩放和平台组合的 macOS 模块。
- 新建：<code>apps/desktop/src/control/permissions.ts</code> 及测试。
- 修改：helper fixture 和集成测试。

- [ ] **步骤 1：先写与平台无关的 fake 测试，覆盖 app/窗口身份、AX role/name/bounds 投影、安全值脱敏、缩放转换、捕获范围、权限拒绝和过期窗口 ref。**
- [ ] **步骤 2：实现带深度/节点/文本限制的 AXUIElement 遍历。绝不返回安全字段值或隐藏窗口内容。**
- [ ] **步骤 3：要求 macOS 14+，并且只通过使用准确窗口 <code>SCContentFilter</code> 和有界 <code>SCStreamConfiguration</code> 的 <code>SCScreenshotManager</code> 捕获已授权窗口。CoreGraphics 可以解析窗口身份/坐标并支持 CGEvent，但绝不截图或作为 fallback。**
- [ ] **步骤 4：从实际调用 API 的 helper 查询辅助功能和屏幕录制权限。返回手动系统设置指引；不要自动修改隐私设置。**
- [ ] **步骤 5：运行 Cargo 测试，并针对两个无害 app 运行一次手动只读 fixture。确认没有发出点击、按键或滚动事件。**
- [ ] **步骤 6：提交 <code>feat(computer): observe macos applications</code>。**

### 任务 11：在清理门禁后启用有界 macOS 输入

**文件：**
- 新建：macOS <code>input.rs</code> 和输入释放测试。
- 修改：helper 状态、策略、Electron 控制器和集成测试。

- [ ] **步骤 1：先写失败测试，覆盖聚焦、语义点击、双击、拖动、Unicode 输入、封闭按键词汇、滚动、每个原生事件之间的取消/撤销、每次原生调用前扣减预算，以及在停止/崩溃/EOF 时释放每个按住的按键/按钮。增加 Electron 保守 input journal 测试：helper 意外退出后，二进制 hash 相同，并且在签名构建中签名身份相同的新 helper 接受 Electron 专用 <code>input.release</code>，其中只包含已知 held key/button 的 up 事件；拒绝子进程/模型尝试。**
- [ ] **步骤 2：证明在任何 CGEvent 调用前就会拒绝安全字段、OTP、生物识别、密码管理器、隐私设置和未经批准的高影响控件。**
- [ ] **步骤 3：启用任何 CGEvent 输入前，先通过 Stop、退出、重试、崩溃和会话 dispose 下“撤销 → 清队列 → 释放 → ack → EOF → 等待 → kill ladder”的原生验收，并通过同 helper 身份恢复释放路径。记录断电/同时强杀的明确限制。**
- [ ] **步骤 4：只有 helper 校验镜像的实时 lease 和新鲜 ref 后，才能实现 CGEvent 操作。在每个发出的原生事件之间检查撤销，并在每次原生调用前扣减事件/动作预算。坐标 fallback 要求支持视觉的路由和当前屏幕截图 revision。**
- [ ] **步骤 5：完成一个可见的双 app fixture 任务；在拖动和输入期间触发紧急停止与 helper 崩溃。确认只回放 journal 中的 up 事件、没有输入卡住，且 Harness 退出后没有 helper。**
- [ ] **步骤 6：提交 <code>feat(computer): control authorized macos apps</code>。**

### 任务 12：实现对称的 Windows 适配器

**文件：**
- 在 <code>native/computer-use-helper/crates/helper/src/platform/windows/</code> 下新建用于 UIA、捕获、权限、缩放、输入和平台组合的 Windows 模块。
- 修改 Windows 原生工作流和 packaged-smoke 测试。

- [ ] **步骤 1：增加 Windows 门控测试，使用 HWND/PID 加进程创建身份拒绝 PID/HWND 重用，并覆盖 UI Automation 投影、安全值脱敏、逐显示器 DPI、过期 ref、Windows Graphics Capture 范围、完整 SendInput 释放，以及在不提权的情况下拒绝 UIPI 阻止项、高完整性目标和 secure desktop。**
- [ ] **步骤 2：先实现 UIA 语义观察和 WGC 捕获，再启用输入。在 Windows 上逐字节验证同一组协议 fixture。**
- [ ] **步骤 3：通过 SendInput 实现聚焦、语义点击、双击、拖动、Unicode 输入、封闭按键组合、滚动、停止和父进程断连清理。绝不提权；在输入前拒绝 UIPI、高完整性、secure desktop 或身份重用目标。在原生事件之间重新检查撤销，并在每次调用前扣减预算。**
- [ ] **步骤 4：只运行一次 Windows 工作流。完成两个无害 app 任务、敏感操作拒绝、紧急停止、app 关闭、Harness 关闭、卸载、进程清理和数据保留。**
- [ ] **步骤 5：提交 <code>feat(computer): control authorized windows apps</code>。**

### 任务 13：增加 Computer Use 工具、设置和全局停止 UI

**文件：**
- 新建：<code>packages/control/tool-computer-control/</code>，包含聚焦模块/测试/README 配对。
- 新建：<code>packages/client/ui-desktop-control/</code>，包含设置、状态胶囊、store、约定、locale、样式和测试。
- 修改：<code>packages/client/ui-layout/src/client/AppFrame.tsx</code> 和 slot 声明。
- 修改：<code>apps/desktop/src/preload-api.ts</code>、<code>preload.ts</code>、<code>main.ts</code>、菜单/托盘文件、偏好设置和测试。
- 修改：<code>apps/desktop/desktop.cordis.patch.yml</code> 和 staging 要求。

- [ ] **步骤 1：先为独立的封闭工具编写失败测试，覆盖 <code>computer_status</code>、<code>computer_list</code>、<code>computer_snapshot</code>、<code>computer_focus</code>、<code>computer_click</code>、<code>computer_double_click</code>、<code>computer_drag</code>、<code>computer_type</code>、<code>computer_key</code>、<code>computer_scroll</code>、<code>computer_wait</code> 和无需批准的 <code>computer_stop</code>。**
- [ ] **步骤 2：先写失败的 UI 测试，覆盖操作系统权限状态、app allowlist、当前 app/操作状态胶囊、Stop、托盘/菜单 Stop、可配置紧急快捷键，以及无需重启即可加载的 marketplace 状态。0.4.3 没有 Pause 状态、按钮或协议动作。**
- [ ] **步骤 3：仅为支持视觉的路由通过附件保存屏幕截图。明确从遥测、日志、memory 和 MSE candidate 输入中排除屏幕截图/AX 文本。**
- [ ] **步骤 4：增加叠加式布局状态 slot，但不接管现有 overlay slot。仅在控制活跃时注册全局停止快捷键，并在停止或 app dispose 时注销。**
- [ ] **步骤 5：运行所有控制、client、GUI、可等待的 <code>DesktopApplication</code> 关闭、生命周期、plugin、memory、MSE 和启动测试。覆盖退出/重试/崩溃/会话 dispose，并证明控制关闭先于 Harness stop。确认权限缺失、provider 不存在或 helper 失败绝不会阻止 Harness 启动或普通 CLI/Web 聊天。**
- [ ] **步骤 6：提交 <code>feat(desktop): finish visible computer use</code>。**

### 任务 14：设置版本、编写文档、验证、安装并发布 0.4.3

**文件：**
- 修改全部三个 Desktop 包 manifest、更新元数据、版本/迁移/manifest 测试、发布工作流、packaged-smoke sentinel、根/Desktop README 配对、<code>PROJECT_CONTEXT.md</code>、生成目录、模块图、第三方通知和更新 manifest。
- 增加已实现的 Agent Note 双语配对，并将任何 proposed note 移至 implemented。
- 构建：<code>DeepSeek-Harness-0.4.3-mac-x64.dmg</code> 和 <code>DeepSeek-Harness-Setup-0.4.3-win-x64.exe</code>。

- [ ] **步骤 1：先把版本断言改为 0.4.3，并在修改生产 manifest 前确认进入 RED。**
- [ ] **步骤 2：从同一个最终源代码提交更新 Desktop 拥有的每一个版本表面和文档。如果没有签名凭据，允许交付明确标记为未签名/未公证的测试版，但不承诺 TCC 持久或 SmartScreen 信任。配置签名时，验证嵌套 helper 与 app 使用同一个 Team ID 签名、启用 hardened runtime 并公证；验证签名 Windows helper 与签名 NSIS 安装器，并明确 SmartScreen 状态。**
- [ ] **步骤 3：运行聚焦测试、Cargo fmt/clippy/tests、<code>test:gui</code>、回放 Web 测试、typecheck、lint、constraints、生成目录、模块图、文档同步、密钥扫描和干净源码构建。**
- [ ] **步骤 4：使用临时 <code>DSH_HOME</code> 构建并验证 Intel macOS 14+ DMG，执行 packaged smoke、嵌套 helper 身份/架构检查、<code>hdiutil verify</code>、校验和、浏览器循环、双 app Computer Use 循环、敏感操作拒绝、紧急停止、零残留进程和真实数据不变证据。在共享原生工作流中，配置签名时验证签名 Windows helper/NSIS 路径；否则验证并标记未签名测试路径及其 SmartScreen 限制。**
- [ ] **步骤 5：备份已安装 app，仅退出准确拥有的进程，替换 app，验证版本/架构/随机回环启动/控制权限/数据保留，并保留恢复备份直至验收通过。**
- [ ] **步骤 6：只 push 一次，从准确的 0.4.3 tag 分发一个共享原生发布工作流，不重试或重新下载未变化的产物。**
- [ ] **步骤 7：仅在 Mac 和 Windows 原生门禁通过后发布。验证 uploaded 状态、准确大小、SHA-256、LF 校验和文件、更新 manifest 字节/哈希/URL、保留无关资产，以及每个平台一次匿名下载。**
- [ ] **步骤 8：提交最终证据 <code>docs(desktop): close 0.4.3 release</code>。**

## 最终自查清单

- 已批准设计中的每项要求都映射到上述某项任务。
- 任何任务都不增加回退、消息编辑、任意选择器、任意 JavaScript、基于 shell 的 GUI 控制、凭证读取、静默控制或后台屏幕截图。
- 浏览器 ref、AX/UIA ref、屏幕截图、租约和批准都有唯一的权威拥有者与明确的到期时间。
- 在启用原生输入前，实现并测试停止和清理。
- macOS 与 Windows 从同一提交发布，但使用真实原生适配器和真实原生验收。
- 版本设置和 GitHub 访问只在最后发生一次，并且要在本地门禁之后。
